# Obsidian Pages Plugin UI 原型规格

> 原型问题：Obsidian 中的本地优先发布插件，应该通过哪些界面让用户安全地建立站点、审阅变化、配置文章并完成整站发布？
>
> 结论状态：ASCII 原型已逐页确认，作为首版信息架构与交互基线。
>
> 更新日期：2026-07-31

## 0. 阅读约定

本文件记录结构、信息层级、状态和交互，不规定像素级视觉效果。具体视觉规则见 [`DESIGN.md`](./DESIGN.md)。

ASCII 符号：

- `[ 操作 ]`：按钮。
- `[✓] / [ ]`：内容是否进入下一次完整站点构建。
- `▾`：下拉菜单或可展开区。
- `● / ▲ / ✕`：成功、警告、阻塞/失败；实现时使用 Lucide 图标而非字符。
- `···`：更多操作菜单。
- `│`：布局分隔，不代表必须使用可见粗边框。

所有示例数据均为原型数据，不是默认配置。

## 1. 整体信息架构

```text
Obsidian
├─ Ribbon：发布入口
│  └─ 根据状态打开：首次设置 / 环境准备 / 发布中心 / 发布进度
├─ 主工作区
│  ├─ 首次设置
│  └─ 发布中心
├─ 右侧边栏
│  └─ 当前文章发布面板
├─ 设置
│  └─ Pages Publish
├─ 命令面板
├─ Markdown 文件右键菜单
├─ 状态栏
└─ Notice
```

主流程：

```text
未配置
  │ Ribbon / 命令
  ▼
准备本地环境 ──失败──> 修复 / 重试
  │
  ▼
首次设置：站点信息 → 内容范围 → Cloudflare → 确认
  │                                  │
  │                                  └─ 只创建与扫描，不发布文章
  ▼
发布中心：扫描 → 审阅变化 → 修复阻塞 → 预览 → 发布
  │                                           │
  │                                           ├─ 成功：更新线上事实
  │                                           └─ 失败：线上保持不变
  └─ 当前文章面板 / 设置页提供上下文编辑
```

## 2. 发布中心

### 2.1 宿主与目的

- 位置：Obsidian 主工作区 `ItemView`。
- 目的：查看整站待发布变化、检查完整性、逐项审阅并执行原子发布。
- 唯一 Primary：`发布站点`。
- 不采用 Modal 或左/右侧栏承载完整发布流程。

### 2.2 完整页面原型

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 发布中心                                                                     │
│ LLM Wiki                                                                     │
│ https://llm-wiki.pages.dev  ● Cloudflare 已连接  上次发布 2 小时前           │
│                                                     [打开站点] [···]         │
├──────────────────────────────────────────────────────────────────────────────┤
│ 自上次发布以来                                                               │
│  12 项变化       +3 新增       ~7 更新       -2 待下线                       │
│  ✕ 1 个阻塞      ▲ 3 个警告                         扫描于 14:32 [重新扫描] │
│                                                                              │
│ ✕ 发布被阻塞：guide/agents.md 引用了一张私密图片。             [查看问题]   │
├──────────────────────────────────────────────────────────────────────────────┤
│ [当前变化 12]  [全部内容 86]  [未发布 9]  [问题 4]             搜索… [筛选] │
├────┬────────────────────────────┬──────────┬────────────┬────────────────────┤
│上线│ 文章 / 路径                │公开方式  │状态变化    │检查                │
├────┼────────────────────────────┼──────────┼────────────┼────────────────────┤
│ ✓  │ Agent 设计                 │公开      │新增        │● 通过              │
│    │ notes/agent-design.md      │          │            │                    │
├────┼────────────────────────────┼──────────┼────────────┼────────────────────┤
│ ✓  │ RAG 指南                   │不列出    │内容更新    │▲ 2 个警告          │
│    │ guide/rag.md               │          │            │                    │
├────┼────────────────────────────┼──────────┼────────────┼────────────────────┤
│ ✓  │ Agent 工具                 │公开      │URL 已变化  │● 自动重定向        │
│    │ guide/tools.md             │          │            │                    │
├────┼────────────────────────────┼──────────┼────────────┼────────────────────┤
│ ✓  │ 多智能体协作               │公开      │内容更新    │✕ 1 个阻塞          │
│    │ guide/agents.md            │          │            │                    │
├────┼────────────────────────────┼──────────┼────────────┼────────────────────┤
│    │ 旧版简介                   │—         │待下线      │▲ 将从线上移除      │
│    │ archive/intro.md           │          │            │                    │
└────┴────────────────────────────┴──────────┴────────────┴────────────────────┘
┌──────────────────────────────────────────────────────────────────────────────┐
│ ✕ 仍有 1 个阻塞问题。现有线上站点不会受到影响。                              │
│                                              [预览站点] [发布站点（不可用）] │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 页面头部

```text
发布中心
LLM Wiki
https://llm-wiki.pages.dev  ● Cloudflare 已连接  上次发布 2 小时前
                                                    [打开站点] [···]
```

规则：

- “发布中心”是页面类型；站点名是主标题。
- 展示主域名、连接状态与最近一次成功发布时间。
- `打开站点` 只有存在已部署站点时可用。
- `···` 可包含打开设置、打开配置文件、复制站点 URL、查看诊断；不放发布动作。
- 未连接、配置失效、从未发布时用状态替换对应字段，不保留假数据。

### 2.4 变化摘要与筛选

```text
自上次发布以来
 12 项变化   +3 新增   ~7 更新   -2 待下线
 ✕ 1 个阻塞  ▲ 3 个警告                    扫描于 14:32 [重新扫描]

✕ 发布被阻塞：guide/agents.md 引用了一张私密图片。       [查看问题]
```

- 指标可点击，点击后筛选下方列表。
- “变化”相对于最近一次成功发布，不相对于最近一次扫描。
- 重新扫描只读取 Vault 和配置，不写 Frontmatter、不触发发布。
- 只有 Blocker 禁用发布；Warning 允许继续。
- 多个 Blocker 时摘要显示数量和最高优先级问题，`查看问题` 切换到问题视图。

### 2.5 内容列表

Tabs：

- `当前变化`：新增、更新、URL 变化、可见性变化、待下线。
- `全部内容`：所有内容范围中的 Markdown 与仍在线但已移出范围的内容。
- `未发布`：符合候选条件但没有线上版本的内容。
- `问题`：有 Blocker 或 Warning 的内容。

Checkbox 语义：

- `[✓]` 表示这篇内容应出现在下一版完整站点中。
- `[ ]` 不是“本次不更新”，而是“下一版不包含”。
- 取消一篇已上线内容会产生“待下线”确认。
- 首次设置完成后，可以为扫描出的合格候选提供批量初始选择；之后新增笔记默认 `private`，不自动加入线上站点。
- 线上内容移动出范围或文件缺失时仍显示为“待下线”，防止静默消失。

点击行打开右侧审阅抽屉，点击 Checkbox、URL、错误定位或菜单不触发行点击。

### 2.6 右侧审阅抽屉

```text
                                    ┌────────────────────────────────────────┐
                                    │ 多智能体协作                      [×] │
                                    │ guide/agents.md                      │
                                    ├────────────────────────────────────────┤
                                    │ 发布结果                              │
                                    │ [✓] 下一版包含此文章                  │
                                    │ 公开方式  [公开 ▾]                    │
                                    │ 待发布 URL                            │
                                    │ /guide/multi-agent-collaboration/     │
                                    │                         [编辑 URL]     │
                                    │ 当前线上 URL                          │
                                    │ /guide/agents/                        │
                                    │ ● 发布后自动保留旧地址重定向          │
                                    ├────────────────────────────────────────┤
                                    │ 检查                                  │
                                    │ ✕ 私密图片                            │
                                    │   ![[private/team-map.png]] · 第 28 行 │
                                    │   图片不会被复制，发布被阻塞。         │
                                    │                         [定位到原文]   │
                                    │ ▲ 私密文章引用                        │
                                    │   [[internal-plan]] · 第 42 行         │
                                    │   发布后降级为普通文字。               │
                                    ├────────────────────────────────────────┤
                                    │ 将写入 Frontmatter                    │
                                    │ publication.visibility: public        │
                                    │ publication.slug: ...                 │
                                    │ redirects: /guide/agents/             │
                                    ├────────────────────────────────────────┤
                                    │ 依赖  图片 4 · 笔记 7 · 外链 3        │
                                    │                              [展开]   │
                                    └────────────────────────────────────────┘
```

抽屉顺序固定为：发布结果 → 检查 → Frontmatter 变化 → 依赖。

- 同时展示“待发布 URL”和“当前线上 URL”，避免把意图误认为事实。
- URL 编辑遵循 slug 校验；已知旧 URL 自动写入 redirects。
- 每个问题显示源文件、精确行号、影响与定位动作。
- 抽屉内修改是发布意图，可以立即写入对应文章 Frontmatter；写入成功不等于已上线。
- 切换行时保留列表滚动、Tab 和筛选。

### 2.7 固定发布条与阶段

阻塞：

```text
✕ 仍有 1 个阻塞问题。现有线上站点不会受到影响。
                                    [预览站点] [发布站点（不可用）]
```

可发布：

```text
12 项变化将发布：3 新增 · 7 更新 · 2 下线
                                              [预览站点] [发布站点]
```

进行中：

```text
发布中：构建与检查（第 2/4 阶段）
准备 ✓  →  构建与检查 ●  →  上传  →  激活
                                                        [查看日志]
```

成功：

```text
● 发布成功 · 86 篇文章 · 用时 38 秒 · 14:41
                                         [打开站点] [查看本次结果]
```

失败：

```text
✕ 上传失败。新版本未激活，现有线上站点保持不变。
Cloudflare 请求超时。                         [查看详情] [重试发布]
```

约束：

- 阶段只有“准备、构建与检查、上传、激活”，不展示推测百分比。
- 上传开始后不显示“取消”。
- 发布失败不写部署成功事实，也不回滚用户已经明确保存的发布意图。
- 重试基于重新验证后的当前 Vault 状态，不盲目复用陈旧构建。

### 2.8 窄容器

```text
┌────────────────────────────────────┐
│ [✓] Agent 设计                     │
│ notes/agent-design.md              │
│ 新增 · 公开              ● 通过   │
├────────────────────────────────────┤
│ [✓] 多智能体协作                   │
│ guide/agents.md                    │
│ 内容更新 · 公开       ✕ 1 个阻塞  │
└────────────────────────────────────┘
```

次要列折叠进条目元数据；不使用横向滚动。审阅抽屉覆盖列表，并提供明确返回按钮。

## 3. 首次设置

### 3.1 流程骨架

```text
┌──────────────────────────────────────────────────────────────────────┐
│ Pages Publish                                                       │
│ 创建你的发布站点                                                    │
│                                                                    │
│ ● 环境准备   1 站点信息   2 内容范围   3 Cloudflare   4 确认       │
├──────────────────────────────────────────────────────────────────────┤
│                         当前步骤内容                                │
├──────────────────────────────────────────────────────────────────────┤
│                                              [返回] [继续]          │
└──────────────────────────────────────────────────────────────────────┘
```

- 首次设置位于主工作区，是聚焦式向导。
- 先自动准备/验证兼容 Node 与本地发布引擎，再进入四个用户步骤。
- 过程中只保存本地草稿；最终确认前不创建远端项目、不写正式 `site.yml`。
- 离开向导时可保留草稿，并清楚说明尚未创建站点。

### 3.2 环境准备

```text
准备本地发布环境

● 检查系统与 Vault
● 查找兼容的 Node.js 运行时
● 准备 Pages 发布引擎
○ 验证本地预览服务

正在准备 Pages 发布引擎…
此过程不会修改你的系统 Node.js 配置。
                                                    [查看详情]
```

失败时显示失败步骤、可理解原因、诊断入口和重试；不要求普通用户手动选择 Node 路径。高级修复入口放在详情中。

### 3.3 第 1 步：站点信息

```text
站点信息

站点名称 *
[ LLM Wiki                                                   ]
默认使用 Vault 名称。支持中文；站点名称不会决定域名。

站点简介
[ 关于 LLM、Agent 与知识管理的个人笔记                   ]
[                                                             ]
0 / 160

                                    [退出设置] [继续：内容范围]
```

- 站点名称必填，预填 Vault 名称。
- 简介可选，最多 160 字符。
- 此步骤不要求选择域名，也不把名称强制 slug 化。

### 3.4 第 2 步：内容范围

```text
内容范围
选择哪些 Vault 目录可以成为发布候选，并设置它们的公开路径。

┌──────────────────────────┬──────────────────────┬──────────────┐
│ Vault 目录               │ 公开路径             │ 扫描结果     │
├──────────────────────────┼──────────────────────┼──────────────┤
│ notes/                   │ /notes/              │ 42 篇        │
│ guide/                   │ /guide/              │ 18 篇        │
└──────────────────────────┴──────────────────────┴──────────────┘
                                             [添加内容目录]

示例 URL
guide/agents.md  →  /guide/agents/

● 找到 60 篇 Markdown 候选
▲ 3 篇目录索引页将单独处理

                              [返回] [继续：Cloudflare]
```

校验：

- 内容目录不能彼此重叠。
- 公开路径不能冲突。
- 可以选择 Vault 根目录，但必须先确认更大的暴露范围。
- 只有 Markdown 是内容候选；本地图片由引用关系收集，不独立成为候选。
- `_index.md` 是首选目录索引；若与 `index.md` 同时存在，按既定优先级处理，不额外制造警告。
- 扫描结果和示例 URL 在用户继续前可见。

### 3.5 第 3 步：Cloudflare

未连接：

```text
Cloudflare
用于创建或绑定 Pages 项目并发布站点。

[使用 Cloudflare 登录]
将在浏览器中打开授权页面。凭据只保存在系统 Keychain。

高级方式
▸ 使用 API Token

                                     [返回] [继续（不可用）]
```

已连接：

```text
Cloudflare

● 已连接：ivan@example.com                    [更换账号]

账号
[ Ivan Personal ▾ ]

Pages 项目
(●) 创建新项目   ( ) 绑定已有项目
项目标识
[ llm-wiki                            ] [检查可用性]
● llm-wiki 可用

域名
(●) 先使用 llm-wiki.pages.dev
( ) 连接自定义域名

                                          [返回] [继续：确认]
```

- OAuth 是主路径；API Token 放在高级方式。
- 账号、项目和域名都在最终确认前保持为计划，不提前修改远端。
- 绑定已有项目时必须验证兼容性与归属。
- 自定义域名流程说明 DNS/验证状态，但不承诺即时生效。

### 3.6 第 4 步：确认

```text
确认创建站点

站点
LLM Wiki                                             [编辑]
关于 LLM、Agent 与知识管理的个人笔记

内容范围
notes/  → /notes/      42 篇                        [编辑]
guide/  → /guide/      18 篇

Cloudflare
Ivan Personal · 新项目 llm-wiki                     [编辑]
llm-wiki.pages.dev

将执行
• 在本地写入 .publish/site.yml
• 创建并绑定 Cloudflare Pages 项目
• 扫描可发布候选

不会执行
• 不会发布任何文章
• 不会修改文章 Frontmatter

                         [返回] [创建站点并开始扫描]
```

执行阶段：

```text
正在创建站点
● 写入本地配置
● 创建 Cloudflare Pages 项目
● 保存安全绑定
○ 扫描内容候选

请保持 Obsidian 运行。                         [查看详情]
```

成功：

```text
● 站点已创建
找到 60 篇候选，其中 57 篇可以加入首次发布。
没有文章被发布，也没有修改文章 Frontmatter。

                                         [进入发布中心]
```

失败：

- 明确列出已成功与未完成步骤。
- 重试必须幂等，优先复用已经创建且匹配的远端项目。
- 如果正式本地配置未能完成，则不把向导标记为成功。

## 4. 插件设置

### 4.1 宿主与结构

- 位置：Obsidian `Settings → Community plugins → Pages Publish`。
- 单页滚动，不使用 Tab 隐藏配置。
- 锚点：站点、内容范围、Cloudflare、站点功能、本地环境。
- 普通本地更改统一由底部 `保存设置` 提交；远端动作独立执行。

### 4.2 完整页面原型

```text
Pages Publish
LLM Wiki · https://llm-wiki.pages.dev
● 配置有效 · .publish/site.yml          [打开配置文件] [打开发布中心]

[站点] [内容范围] [Cloudflare] [站点功能] [本地环境]

──────────────────────────────── 站点 ────────────────────────────────
站点名称
[ LLM Wiki                                                       ]
站点简介
[ 关于 LLM、Agent 与知识管理的个人笔记                           ]

────────────────────────────── 内容范围 ─────────────────────────────
当前候选 60 篇 · 已上线 48 篇
┌──────────────────────┬────────────────┬──────────────┬────────────┐
│ Vault 目录           │ 公开路径       │ 当前候选     │            │
├──────────────────────┼────────────────┼──────────────┼────────────┤
│ notes/               │ /notes/        │ 42           │ [编辑] [×] │
│ guide/               │ /guide/        │ 18           │ [编辑] [×] │
└──────────────────────┴────────────────┴──────────────┴────────────┘
[添加内容目录]

────────────────────────────── Cloudflare ────────────────────────────
账号      ● Ivan Personal                           [重新授权]
项目      llm-wiki                                  [在 Cloudflare 打开]
主域名    llm-wiki.pages.dev
自定义域名 docs.example.com · ▲ 等待 DNS 验证       [检查状态]
[连接自定义域名]

────────────────────────────── 站点功能 ─────────────────────────────
首页布局
(●) 按目录展示分区    ( ) 按最新文章展示

[✓] 全文搜索
[✓] 知识图谱

────────────────────────────── 本地环境 ─────────────────────────────
Node.js       ● 兼容运行时 22.x · 由插件管理
发布引擎      ● 0.1.x · 已就绪                       [检查更新]
本地预览      未运行                                 [启动预览]

[修复本地环境] [打开缓存目录] [打开日志] [导出诊断包]

──────────────────────────────────────────────────────────────────────
有未保存的设置。保存后将重新扫描，但不会自动发布。
                                      [放弃更改] [验证] [保存设置]
```

### 4.3 头部状态

有效：

```text
● 配置有效 · .publish/site.yml       [打开配置文件] [打开发布中心]
```

配置文件外部修改：

```text
▲ .publish/site.yml 已在外部修改。本页尚未保存的内容可能过期。
                                      [重新载入] [查看差异]
```

无配置或语法错误：

```text
✕ 无法读取站点配置：第 18 行缩进无效。发布已暂停。
                              [打开并定位] [从有效备份恢复]
```

- `.publish/site.yml` 是站点设置的唯一来源。
- 外部修改冲突时禁止直接覆盖，先重新载入或查看差异。
- 不把 UI 内缓存当成第二份权威配置。

### 4.4 站点与站点功能

- 可编辑名称和 160 字以内简介。
- 首页布局只能选择“目录分区”或“最新文章流”。
- 首版仅展示全文搜索与知识图谱开关。
- 评论、分析、多主题等未实现能力不显示禁用占位。
- 发布完整性、安全凭据存储等系统不变量不可配置为关闭。

### 4.5 内容范围变更

编辑公开路径：

```text
编辑公开路径
guide/：/guide/ → /handbook/

影响：18 篇候选中，12 个线上 URL 将变化。
保存后会为已知旧 URL 自动保留重定向；不会自动发布。
                                             [取消] [应用更改]
```

移除内容范围：

```text
移除 guide/？
12 篇已上线内容将在下一次发布时下线，本地文件不会删除。
                                  [取消] [移除并标记待下线]
```

整根目录暂时缺失：

```text
✕ 内容目录 guide/ 当前不存在。为防止意外批量下线，发布已阻塞。
                                      [重新扫描] [编辑内容范围]
```

- 暂时卸载同步盘或重命名根目录不能被立即解释为用户要批量下线。
- 用户明确移除内容范围后，才形成下一次发布的下线意图。

### 4.6 Cloudflare 远端动作

- 重新授权、更换账号、绑定项目、连接域名、检查 DNS 是独立动作。
- 动作前展示目标账号/项目；失败时本地现有绑定保持不变。
- 普通 `保存设置` 不创建、删除或重绑远端项目。
- 不提供“删除 Cloudflare 项目”。
- 自定义域名显示状态：待验证、有效、失败，并给出下一步。

### 4.7 本地环境与危险操作

- 展示实际使用的 Node 来源、版本与兼容状态。
- 修复流程可以重新下载插件管理的运行时/引擎，但不改用户系统 Node。
- 导出诊断包默认排除密钥、文章正文和私密资源内容。
- 清理缓存必须说明缓存会自动重建。
- 移除本地站点配置时，将 `site.yml` 移到 Obsidian 回收站；不删除远端站点或线上内容。

### 4.8 保存过程

```text
[验证设置] → [展示 URL/下线影响] → [原子写入 site.yml] → [重新扫描]
```

- 校验或原子写入失败时保留用户输入，旧配置继续有效。
- 保存成功只提示“配置已保存并完成扫描”，不能提示“发布成功”。
- 重新扫描发现 Blocker 时导航到发布中心问题视图，不自动部署。

## 5. 当前文章发布面板

### 5.1 宿主与导航

- 位置：Obsidian 右侧边栏。
- 默认跟随当前活动 Markdown 文件；用户可以固定到某一文件。
- 可通过 Ribbon/命令面板/文件右键菜单打开。
- Primary 是 `预览当前文章`；另有 `打开发布中心`，无直接发布按钮。

### 5.2 完整面板原型

```text
┌──────────────────────────────────────┐
│ 当前文章发布                    [📌] │
├──────────────────────────────────────┤
│ 多智能体协作                         │
│ guide/agents.md                      │
│ ▲ 有更新 · 等待下一次发布            │
│ 线上版本：2 小时前                   │
├──────────────────────────────────────┤
│ 发布                                 │
│ 公开方式                             │
│ [公开 ▾]                             │
│ 任何人可访问，并出现在列表与搜索中。 │
│                                      │
│ 待发布 URL                           │
│ /guide/multi-agent-collaboration/    │
│                              [编辑]  │
│ 当前线上 URL                         │
│ /guide/agents/              [打开]   │
│ ● 发布后自动保留旧地址重定向         │
├──────────────────────────────────────┤
│ 检查                         [重查]  │
│ ✕ 私密图片 · 第 28 行                │
│   ![[private/team-map.png]]          │
│   发布被阻塞。             [定位]    │
│ ▲ 私密文章引用 · 第 42 行            │
│   发布后降级为普通文字。   [定位]    │
│                                      │
│ 依赖                                 │
│ 图片 4  · 笔记 7 · 外链 3   [展开]  │
├──────────────────────────────────────┤
│ 发布属性                             │
│ 标题  多智能体协作                   │
│       来自首个 H1            [覆盖]  │
│ 摘要  自动提取正文开头        [覆盖]  │
│ 日期  2026-07-31 · 来自文件属性      │
│ 标签  agent, workflow        [编辑]  │
│ 封面  未设置                 [选择]  │
│ ▸ 高级：类型、排序、重定向           │
│ ▸ 部署事实（只读）                   │
├──────────────────────────────────────┤
│ [预览当前文章]                       │
│ [打开发布中心]                       │
└──────────────────────────────────────┘
```

实现时图钉使用 Lucide 图标，不使用 emoji。

### 5.3 身份与状态

标题来源优先级：

```text
publication.title → 首个 H1 → 文件名
```

状态组合：

| 状态 | 主文案 | 辅助说明 |
| --- | --- | --- |
| 未发布 | 未加入发布 | 默认私密或未选择上线 |
| 待首次发布 | 等待首次发布 | 已设置上线意图，但无线上版本 |
| 已同步 | 与线上一致 | 显示最近成功发布时间 |
| 有更新 | 等待下一次发布 | 本地内容较线上版本有变化 |
| 可见性变化 | 可见性待更新 | 当前线上值与待发布值分列 |
| URL 变化 | URL 待更新 | 显示自动重定向状态 |
| 待下线 | 下一次发布将下线 | 需要明确恢复入口 |
| 阻塞 | 无法发布 | 显示最高优先级 Blocker |
| 上次发布失败 | 线上保持旧版本 | 提供发布中心失败详情入口 |

### 5.4 可见性

```text
公开：任何人可访问；出现在列表、搜索和图谱中。
不列出：知道 URL 的人可访问；不出现在列表、搜索和图谱中。
私密：不进入公开构建；已上线内容将在下一次发布时下线。
```

- 用户显式修改后立即写入 `publication.visibility`，这是本地意图。
- 从已上线切换到私密时要求确认“下一次发布将下线”，但不立即改远端。
- 私密文章自身的问题降级为 dormant warning，不阻塞整站；如果它的资源被公开文章依赖，则按依赖规则判断。

### 5.5 URL 与重定向

- 分别显示“待发布 URL”和“当前线上 URL”。
- slug 支持 Unicode。
- 禁止 `/`、`..`、`?`、`#` 等会改变路径层级或 URL 语义的内容。
- 同一公开路径下必须唯一。
- 从界面修改 slug 时，已知旧 URL 自动进入 `redirects`。
- 用户直接编辑 Frontmatter 且无法确认旧 URL 时显示警告，不自动猜测。

### 5.6 检查与依赖

排序：Blocker → Warning → Passed/Info。

每项必须包含：

- 类型与级别。
- 涉及的路径或引用。
- 精确源行。
- 对发布结果的影响。
- `定位` 动作。

依赖分组：本地图片、笔记链接、外部链接。

- 本地图片检查路径、存在性、范围和隐私。
- 私密笔记链接在公开页面中降级为普通文字，不泄露目标 URL。
- 外链可手动触发网络可用性检查；常规本地扫描不依赖网络。
- Blocker 没有“忽略”开关。

### 5.7 发布属性

每个属性同时展示有效值与来源：

| 属性 | 回退来源 | 显式编辑 |
| --- | --- | --- |
| 标题 | 首个 H1 → 文件名 | 写入 `publication.title` |
| 摘要 | 正文摘要规则 | 写入 `publication.summary` |
| 日期 | 文件属性/Frontmatter 规则 | 写入对应 publication 字段 |
| 标签 | 文章现有标签 | 写入显式覆盖字段 |
| 封面 | 无 | 写入本地资源引用 |
| 类型/排序 | 站点默认 | 在高级区覆盖 |
| redirects | 系统已知历史 URL | 可查看并谨慎编辑 |

- 回退值只显示，不自动写入。
- 用户点击“覆盖/编辑”后才创建显式字段。
- 部署事实只读，例如最近线上 URL、内容摘要、成功时间；只在发布成功后由系统更新。

### 5.8 空状态与错误状态

无活动文件：

```text
当前没有活动文章
打开一个 Markdown 文件以查看发布设置。
```

非 Markdown：

```text
此文件不是可发布的 Markdown
Pages Publish 只把 Markdown 作为内容候选。
```

范围外：

```text
此文章不在内容范围内
guide-drafts/ 尚未映射到公开路径。        [打开内容范围设置]
```

已经上线但移出范围：

```text
▲ 此文章当前仍在线，但已移出内容范围
下一次发布前需要确认是恢复范围还是下线。 [查看发布中心]
```

无站点：

```text
尚未创建发布站点                         [开始设置]
```

配置错误：

```text
✕ 站点配置无效，发布功能已暂停
.publish/site.yml 第 18 行缩进无效。      [打开并定位]
```

固定文件丢失：

```text
固定的文章已移动或删除                    [取消固定]
```

本地引擎失败 / Cloudflare 授权失效：

- 文章属性仍可查看。
- 依赖远端或引擎的操作禁用，并显示修复入口。
- 不把连接错误误报成文章内容错误。

## 6. 入口与反馈系统

### 6.1 Ribbon

使用单个 `cloud-upload` 图标作为主入口：

| 当前状态 | 点击行为 | Tooltip/状态 |
| --- | --- | --- |
| 未配置 | 打开首次设置 | 创建发布站点 |
| 环境准备中 | 打开准备进度 | 正在准备发布环境 |
| 已配置、空闲 | 打开发布中心 | 打开发布中心 |
| 发布中 | 打开发布进度 | 发布中：构建与检查 |
| 发布失败 | 打开失败结果 | 上次发布失败，线上保持不变 |

图标本身不持续动画，也不通过颜色独自表达失败。

### 6.2 命令面板

首版命令：

- `Pages Publish: 打开发布中心`
- `Pages Publish: 打开当前文章面板`
- `Pages Publish: 预览当前文章`
- `Pages Publish: 预览站点`
- `Pages Publish: 更改当前文章可见性`
- `Pages Publish: 打开当前文章线上页面`
- `Pages Publish: 打开插件设置`

不提供“立即发布当前文章”“跳过检查发布”等命令。

### 6.3 Markdown 文件右键菜单

```text
Pages Publish  ▸
  打开发布面板
  更改可见性…
  预览文章
  打开线上页面
```

- 只对 Markdown 文件显示。
- 菜单项按文件当前状态启用/禁用，并给出不可用原因。
- 首版不支持多选文件的批量发布菜单。

### 6.4 状态栏

正常空闲时隐藏。以下状态出现：

```text
Pages：正在扫描…
Pages：12 项待发布
Pages：1 个阻塞
Pages：发布中 · 上传
Pages：发布失败
```

- 点击状态栏打开对应上下文：发布中心、问题视图或发布进度。
- 状态栏不显示凭据、完整私密路径或滚动日志。

### 6.5 Notice

只在以下情况使用：

- 用户主动操作成功或失败。
- Cloudflare 授权失效或恢复。
- 配置被外部修改并影响当前编辑。
- 后台发布完成，而用户不在发布中心。

不为每次自动扫描、每个 Warning 或每篇文章状态变化弹 Notice。

示例：

```text
发布成功：86 篇文章已上线。                     [打开站点]
发布失败：新版本未激活，现有站点保持不变。     [查看详情]
Cloudflare 授权已失效，发布已暂停。             [重新授权]
```

### 6.6 快捷键

- 不默认注册危险的发布快捷键。
- 用户可以通过 Obsidian 自行给安全命令绑定快捷键。
- 即使用户为“打开发布中心”设置快捷键，真正发布仍需在发布中心确认。

## 7. 跨界面状态一致性

| 事件 | 发布中心 | 当前文章面板 | 设置页 | 状态栏/Notice |
| --- | --- | --- | --- | --- |
| 修改文章可见性 | 变化计数更新 | 显示待发布值 | 无变化 | 有待发布变化时显示状态栏 |
| 保存站点设置 | 重新扫描并更新影响 | 重新计算范围/URL | 显示保存成功 | 不提示发布成功 |
| 发现 Blocker | 禁用发布并定位 | 当前文章相关时展示 | 配置类错误显示 | 状态栏显示数量 |
| 开始发布 | 固定条显示阶段 | 显示“发布进行中” | 远端绑定操作禁用 | 状态栏持续可见 |
| 发布成功 | 清空对应变化、更新时间 | 状态变为已同步 | 无配置变化 | 一次成功 Notice（必要时） |
| 发布失败 | 保留变化、线上事实不变 | 显示失败且线上仍为旧版 | 无配置变化 | 一次失败 Notice |
| Cloudflare 授权失效 | 发布禁用 | 远端动作禁用 | 提供重新授权 | 状态栏/一次 Notice |
| 内容根暂时缺失 | 防批量下线 Blocker | 相关文章显示范围异常 | 显示根目录错误 | 状态栏显示 Blocker |

## 8. 实现时必须覆盖的原型状态

### P0：发布安全

- 正常变化、Warning、Blocker 三类发布前状态。
- 已上线内容取消选择后的待下线确认。
- 发布四阶段、成功、构建失败、上传失败、激活失败。
- 失败后线上事实与最近成功时间保持不变。
- 内容根暂时缺失时阻止意外批量下线。

### P1：内容编辑

- 未发布、已同步、有更新、URL 变化、可见性变化、待下线。
- slug 冲突、已知旧 URL 自动重定向、未知旧 URL 警告。
- 私密笔记链接降级、私密图片阻塞、缺失图片阻塞。
- Frontmatter 回退值与显式覆盖值的区别。

### P2：环境与连接

- 首次环境准备成功/失败/重试。
- OAuth 成功、授权失效、重新授权、Token 高级路径。
- 配置缺失、YAML 错误、外部编辑冲突。
- 本地预览服务启动失败和发布引擎修复。

## 9. 首版明确不做

- 单篇即时发布或局部远端补丁。
- 发布历史与回滚 UI。
- 评论、分析、多主题和主题编辑器。
- 远端 Cloudflare 项目删除。
- 多选文件右键批量发布。
- 忽略 Blocker 或强制发布。
- 进度百分比、不可兑现的上传取消。

## Source Manifest

### Sources

- [Obsidian 发布插件架构](thread://019fb7b2-bf37-7883-9334-53dac3a35ca1)：站点配置来源、内容模型、发布/失败语义、Cloudflare 集成、安全边界与首版功能范围。
- [规划_插件UI设计_2026-07-31](thread://019fb857-2600-7353-9f31-a9519017a003)：发布中心、首次设置、插件设置、当前文章面板、入口反馈系统与设计系统的逐层 ASCII 对齐过程。
- 用户明确指令（2026-07-31）：生成 `DESIGN.md` 与 `UI-SPEC.MD`，分别保存 UI 设计规范与原型。
- `/Users/ivan/.agents/skills/prototype/SKILL.md` 与 `UI.md`：原型用于回答设计问题，保留已确认答案并将结果持久化。
- 工作区检查（2026-07-31）：仓库除 `.git` 外无既有界面实现；因此当前产物是实现前规格，不是对现有 UI 的描述。

### Produced artifacts

- `/Users/ivan/workspace/ai/obsidian-pages-plugin/DESIGN.md`
- `/Users/ivan/workspace/ai/obsidian-pages-plugin/UI-SPEC.MD`

### Key decisions

- 采用四个主要界面：发布中心、首次设置、插件设置、当前文章面板。
- 发布中心使用变化列表 + 右侧审阅抽屉 + 固定发布条，是唯一整站发布入口。
- 首次设置在最终确认前不修改远端，不发布文章，不修改文章 Frontmatter。
- 设置页本地保存与 Cloudflare 远端动作分离；保存配置只重新扫描，不发布。
- 当前文章面板即时保存用户发布意图，但明确区分待发布值与线上事实。
- Ribbon 是单一主入口，状态栏只在有活动或需关注状态时出现，Notice 保持低噪音。

### Verification evidence

- 文档使用 Markdown 与纯 ASCII 原型，可在普通 Markdown 阅读器中重读。
- 使用 `git diff --check` 检查空白错误，并检查标题结构与文件非空。
- 当前仓库没有可运行插件代码，因此未执行 UI 截图、交互、主题兼容或可访问性测试。

### Open questions / risks

- Frontmatter `publication` 的首版 schema、默认值与迁移边界已在 [`PRODUCT-SPEC.md`](./PRODUCT-SPEC.md) 的 FR-7 固化。
- Cloudflare OAuth、Pages 项目绑定和自定义域名能力需要以实现期 API 验证为准。
- ASCII 原型没有验证真实 Obsidian 容器高度、侧栏最小宽度与主题 CSS 差异；实现后应进行视觉保真循环。
- 首次发布时“合格候选的批量初始选择”需要通过真实大 Vault 数据验证默认策略是否足够安全。
