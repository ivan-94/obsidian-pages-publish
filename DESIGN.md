# Obsidian Pages Plugin UI 设计规范

> 状态：已对齐，可作为首版 UI 实现基线
>
> 更新日期：2026-07-31
>
> 适用范围：macOS 上的 Obsidian 桌面端插件

## 1. 设计目标

这个插件应当像 Obsidian 自带的一套“可信发布工具”，而不是嵌入 Obsidian 的独立网站后台。

核心体验目标：

1. **原生**：继承当前 Obsidian 主题、排版、交互和键盘习惯。
2. **克制**：优先展示发布判断所需的信息，不用装饰性卡片制造层级。
3. **可信**：始终区分本地意图、待发布变化和线上事实；危险动作必须可预见。
4. **可诊断**：阻塞、失败和警告都说明影响、原因与下一步。
5. **渐进披露**：常用信息保持可见，高级字段与诊断信息按需展开。

## 2. 产品界面模型

插件有四个主要界面和一套全局入口/反馈机制：

| 界面 | 宿主位置 | 核心任务 | 主操作 |
| --- | --- | --- | --- |
| 发布中心 | 主工作区 `ItemView` | 审阅整站变化并执行发布 | 发布站点 |
| 首次设置 | 主工作区引导流 | 建立站点、内容范围与 Cloudflare 绑定 | 创建站点并开始扫描 |
| 插件设置 | Obsidian 原生设置页 | 修改长期配置与维护本地环境 | 保存设置 |
| 当前文章面板 | 右侧边栏 | 调整当前文章发布意图并定位问题 | 预览当前文章 |
| 入口与反馈 | Ribbon、命令面板、右键菜单、状态栏、Notice | 到达正确界面并了解后台状态 | 视上下文而定 |

设计边界：

- 只有“发布中心”可以发起整站发布。
- 当前文章面板不提供“单篇立即发布”。
- 设置保存、扫描、预览和远端发布是不同动作，不互相暗中触发。
- 远端资源操作与普通本地设置保存隔离。

## 3. 设计语言

### 3.1 颜色

全部颜色使用 Obsidian 语义变量或其可用的等价变量，不硬编码品牌色，不假定明暗主题。

| 语义 | 用途 | 表现原则 |
| --- | --- | --- |
| 默认 | 正文、普通图标、主要信息 | 继承 `--text-normal` |
| 弱化 | 说明、时间、路径、次要元数据 | 继承 `--text-muted` / `--text-faint` |
| 强调 | 当前选择、链接、主要按钮、待处理焦点 | 继承 `--interactive-accent` |
| 成功 | 已连接、已同步、检查通过、发布成功 | 使用宿主成功语义色 |
| 警告 | 不阻塞但需要关注 | 使用宿主警告语义色，并配图标与文字 |
| 危险 | 阻塞、失败、下线、破坏性动作 | 使用宿主错误语义色，并配图标与文字 |
| 边界 | 分区、表头、输入框轮廓 | 继承 `--background-modifier-border` |

规则：

- 颜色不能是唯一的信息载体；状态同时使用图标、短标签或说明文字。
- `public`、`unlisted`、`private` 是可见性分类，不使用成功/警告/危险色暗示价值判断。
- 只有主操作、当前选择与真正需要注意的状态使用高对比色。
- 大面积背景只区分宿主层级，不为每个模块创建独立彩色卡片。

### 3.2 间距与密度

使用 4px 基准网格：

| Token | 数值 | 典型用途 |
| --- | ---: | --- |
| `space-1` | 4px | 图标与短标签、紧凑行内元素 |
| `space-2` | 8px | 控件内部、相邻元数据 |
| `space-3` | 12px | 表格单元格、紧凑表单行 |
| `space-4` | 16px | 普通分组、页面左右内边距下限 |
| `space-6` | 24px | 一级区块间距 |
| `space-8` | 32px | 页面主段落分隔 |

密度原则：

- 发布中心采用信息密集型工具布局，减少无意义留白。
- 设置页保持单列阅读节奏，表单标签与控件基线对齐。
- 移动/窄容器通过重排降低密度，不以横向滚动保留桌面表格。

### 3.3 排版

- 使用 Obsidian 当前界面字体、字号与行高，不引入外部字体。
- 页面标题只承担当前位置识别；站点名或文章名是信息主标题。
- 层级依靠字号、字重、间距和位置共同表达，避免连续多个粗体标题。
- 路径、URL、Frontmatter key 和日志片段使用宿主等宽字体。
- 长路径、URL 与错误信息必须可换行或中段省略；悬停/聚焦可读完整值。

### 3.4 圆角、边框与阴影

- 圆角继承 Obsidian 控件和卡片半径，不建立插件专属圆角体系。
- 分组优先使用间距和细分隔线；只有可独立操作的容器才使用边框。
- 阴影仅用于浮层、菜单、抽屉和固定操作条，不用于普通内容区块。
- 不使用渐变、玻璃拟态或厚重投影。

### 3.5 图标

- 只使用 Obsidian/Lucide 图标集。
- Ribbon 主入口使用 `cloud-upload` 语义图标。
- 常用语义：检查 `check-circle`、警告 `triangle-alert`、阻塞 `circle-x`、刷新 `refresh-cw`、外链 `external-link`、定位 `locate`、设置 `settings`。
- 纯图标按钮必须有 tooltip 与可访问名称。
- 不用 emoji 作为正式状态图标。

## 4. 布局规范

### 4.1 主工作区

- 发布中心和首次设置占据主工作区，以获得完整宽度和稳定上下文。
- 页面头部负责身份与全局动作；中部负责内容；底部固定条负责当前流程的提交动作。
- 表格表头和发布操作条可以吸附，但不得同时遮挡有效内容。
- 审阅抽屉从右侧覆盖或占据辅助宽度，不改变用户当前筛选与滚动位置。

### 4.2 设置页

- 使用 Obsidian 原生设置页内的单页滚动布局。
- 顶部锚点导航用于快速跳到站点、内容范围、Cloudflare、站点功能、本地环境。
- 不用横向 Tab 隐藏配置影响；远端动作形成清晰的独立操作区。

### 4.3 侧边栏

- 当前文章面板跟随活动 Markdown 文件，可被用户固定。
- 面板宽度不足时使用纵向字段布局，不缩小字号或隐藏关键状态。
- 空状态、范围外状态和配置错误状态替换正文，不保留失效控件。

### 4.4 容器响应式

响应的是 Obsidian 视图容器宽度，而不是操作系统窗口宽度：

| 容器宽度 | 行为 |
| --- | --- |
| `>= 900px` | 完整表格；右侧审阅抽屉可并排或覆盖 |
| `640–899px` | 隐藏/折叠次要列；审阅抽屉使用覆盖层 |
| `< 640px` | 表格行改为纵向条目；操作按钮可堆叠；不出现页面级横向滚动 |

## 5. 组件规范

### 5.1 按钮层级

| 层级 | 用途 | 示例 |
| --- | --- | --- |
| Primary | 一个操作区域中唯一的完成动作 | 发布站点、保存设置、创建站点 |
| Secondary | 安全的辅助动作 | 预览、重新扫描、验证配置 |
| Ghost | 导航或低频轻操作 | 打开文件、定位、编辑 |
| Danger | 明确的破坏性动作 | 确认下线、移除本地站点配置 |

规则：

- 每个固定操作区域最多一个 Primary。
- 禁用按钮旁必须给出不可操作原因；不能只依赖 tooltip。
- 破坏性确认描述对象、数量、线上影响和可恢复方式。
- 操作进行中保留按钮宽度，替换为明确阶段文字，避免布局跳动。

### 5.2 状态标签

状态标签保持短小、可扫描：

- 部署事实：`已同步`、`待首次发布`、`有更新`、`待下线`。
- 检查结果：`通过`、`1 个阻塞`、`3 个警告`。
- 连接状态：`已连接`、`需重新授权`、`未配置`。
- 可见性：`公开`、`不列出`、`私密`。

同一位置不要堆叠三个以上标签；复杂状态改为一句摘要并在详情中展开。

### 5.3 表格与列表

- 不使用斑马纹；使用轻分隔线、悬停态和当前选中态。
- 首列承担最重要的批量意图，末列承担检查与详情入口。
- 表头吸附，列标题使用用户语言，不直接暴露内部字段名。
- 整行可打开详情，但 Checkbox、链接和菜单保持各自独立点击目标。
- 空列表必须说明原因和下一步，而不是只显示“暂无数据”。

### 5.4 表单

- 标签放在控件上方或稳定左侧；窄容器统一转为上方。
- 说明文字写清默认值与线上影响。
- 校验尽量在失焦或用户主动验证后出现，避免输入过程中持续报警。
- 自动推导值显示“来源”，例如“来自首个 H1”或“使用文件名”。
- 只有用户明确编辑的覆盖值才写入 Frontmatter；动态回退值不自动固化。

### 5.5 提示、警告与错误

所有反馈采用同一个文案结构：

> 发生了什么 → 对线上/本地有什么影响 → 用户下一步能做什么

示例：

> `guide/logo.png` 位于私密目录，因此本次发布被阻塞。线上站点保持不变。请移动图片或将其加入公开内容范围。

级别规则：

- **Info**：解释当前行为或不可见的系统规则。
- **Warning**：允许继续，但结果可能不符合预期。
- **Blocker**：继续会破坏完整性，发布按钮禁用，不提供“忽略并发布”。
- **Failure**：动作未完成，明确说明线上是否保持不变以及重试入口。

## 6. 动效与异步反馈

- 普通过渡 120–180ms；只用于展开、抽屉、状态切换与焦点移动。
- 尊重 `prefers-reduced-motion`。
- Ribbon 图标不持续旋转；后台状态由状态栏表达。
- 构建与发布显示真实阶段，不伪造百分比：准备 → 构建与检查 → 上传 → 激活。
- 上传开始后不提供不能兑现的“取消”；用户可以关闭视图，任务状态仍可从 Ribbon/状态栏恢复查看。
- 长任务结束通过状态区和一次 Notice 反馈，不反复弹窗。

## 7. 发布语义与安全不变量

下面的规则高于任何局部视觉便利：

1. 发布是整站完整构建和原子部署，不是若干单篇增量上传。
2. 发布列表中的 Checkbox 表示“下一版本是否包含此内容”，不是“本次跳过更新”。
3. 取消已上线内容等同于待下线，必须确认线上影响。
4. 发布失败不更新部署事实，不写成功时间，不改变现有线上站点。
5. 设置保存只更新本地配置并重新扫描，不自动部署。
6. 用户修改可见性、slug 等“发布意图”可以立即写入 Frontmatter；线上状态和时间戳只能在部署成功后写入。
7. URL 变化自动维护重定向；无法识别旧 URL 时先警告。
8. 私密文章引用降级为普通文字链接；私密或范围外的本地图片会阻塞发布。
9. 不允许绕过完整性 Blocker。
10. 凭据只存系统 Keychain，界面与诊断包都不显示密钥。

## 8. 文案与术语

统一使用以下用户语言：

| 使用 | 不使用 | 原因 |
| --- | --- | --- |
| 发布中心 | Dashboard / 控制台 | 直接表达任务 |
| 发布站点 | Deploy / Push | 面向内容作者 |
| 待下线 | 删除 | 区分线上结果与本地文件删除 |
| 内容范围 | Content roots | 对用户隐藏实现术语 |
| 公开路径 | Public root | 中文界面保持一致 |
| 阻塞 | Error（泛称） | 表达是否允许继续 |
| 线上页面 | Production artifact | 直接表达结果 |

文案约束：

- 动作按钮使用动词 + 对象，例如“打开站点”“重新扫描”“确认下线”。
- 不把“成功”用于尚未部署的本地配置保存。
- 删除/移除操作必须明确是本地配置、线上内容还是远端项目。
- 时间同时表达绝对或相对语义，悬停可查看完整时间。

## 9. 可访问性

- 所有功能可通过键盘完成，Tab 顺序与视觉顺序一致。
- 焦点样式使用宿主可见焦点环，不清除 outline。
- 图标按钮具备 `aria-label`；状态变化使用适度的 live region。
- 表格在窄容器改为列表后仍保留字段标签与语义顺序。
- 错误与警告不只依赖颜色；输入错误与对应字段程序化关联。
- 点击目标至少符合 Obsidian 原生控件尺寸；紧凑表格中的小图标提供足够热区。
- 长路径、中文标题、Unicode slug、混合中英文和 200% 缩放都必须可用。

## 10. 首版非目标

- 不建立插件自有主题或自定义字体。
- 不提供多主题商城、评论、分析或主题编辑器占位入口。
- 不提供远端项目删除。
- 不提供插件内历史版本浏览或回滚界面。
- 不提供多选文件的右键批量发布操作。
- 不提供绕过检查的强制发布。
- 不提供虚假的单篇即时发布模型。

## 11. 实现验收清单

实现任一界面前后都应检查：

- [ ] 在 Obsidian 明暗主题中继承宿主颜色与字体。
- [ ] 每个操作区域只有一个 Primary。
- [ ] 关键状态均有图标/文字，不仅依赖颜色。
- [ ] 禁用动作在界面中解释原因。
- [ ] 640px 以下无页面级横向滚动。
- [ ] 键盘可以到达并触发全部操作。
- [ ] 关闭/重开视图不会丢失长任务的可观察状态。
- [ ] 设置保存、扫描、预览、发布没有隐式串联。
- [ ] 发布失败不改变线上事实或成功时间。
- [ ] 凭据、正文与私密路径不会进入普通日志或诊断包。
- [ ] 文案说明“发生什么、影响什么、下一步是什么”。

## Source Manifest

### Sources

- [Obsidian 发布插件架构](thread://019fb7b2-bf37-7883-9334-53dac3a35ca1)：产品架构、发布语义、内容模型、安全边界与首版范围。
- [规划_插件UI设计_2026-07-31](thread://019fb857-2600-7353-9f31-a9519017a003)：逐页 ASCII 原型讨论与已确认的全局设计系统。
- 用户明确指令（2026-07-31）：生成 `DESIGN.md` 与 `UI-SPEC.MD`，分别保存 UI 设计规范与原型。
- 工作区检查（2026-07-31）：仓库除 `.git` 外无既有产品文件，因此没有需要兼容的现成组件库或页面实现。

### Produced artifacts

- `/Users/ivan/workspace/ai/obsidian-pages-plugin/DESIGN.md`
- `/Users/ivan/workspace/ai/obsidian-pages-plugin/UI-SPEC.MD`

### Key decisions

- 采用 Obsidian-native、克制、可信的发布工具风格，并仅使用宿主语义视觉变量。
- 发布中心是唯一发布入口；文章面板只处理单篇意图、检查与预览。
- 设计以完整构建、原子部署和“发布失败不改变线上事实”为最高交互约束。
- 首版使用主工作区、原生设置页与右侧边栏三类宿主表面，不创建独立 Web 管理后台。

### Verification evidence

- 文档使用 Markdown 编写，并以 `git diff --check` 检查空白错误。
- 当前阶段为设计文档，不存在可运行 UI，因此未执行截图对比、交互测试或可访问性自动化检查。

### Open questions / risks

- 实现阶段需用真实 Obsidian API 验证各语义 CSS 变量在目标最低版本中的可用性。
- 容器断点是设计基线，仍需在真实左右侧栏、分屏与缩放场景中做视觉回归。
- Frontmatter 的首版字段 schema 已在 [`PRODUCT-SPEC.md`](./PRODUCT-SPEC.md) 的 FR-7 固化；实现阶段仍需用迁移 fixture 验证旧字段兼容。
