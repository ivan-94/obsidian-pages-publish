# Pages Publish 产品规格

> 文档状态：Final / 首版实现基线
>
> 产品形态：Obsidian Community Plugin
>
> 目标平台：macOS Obsidian 桌面端
>
> 版本范围：MVP / v1
>
> 更新日期：2026-07-31

## 1. 文档目的

本规格定义 Pages Publish 首版的产品边界、用户流程、领域模型、功能行为、数据契约、失败语义和验收标准。

本文件是首版产品行为的总入口：

- UI 视觉语言以 [`DESIGN.md`](./DESIGN.md) 为准。
- 页面信息架构、ASCII 原型和界面状态以 [`UI-SPEC.MD`](./UI-SPEC.MD) 为准。
- 如果三份文档发生冲突，以本文件中的产品语义和安全不变量为最高优先级；视觉与布局细节仍由对应 UI 文档决定。

## 2. 产品摘要

Pages Publish 帮助 Obsidian 用户把 Vault 中明确选择的 Markdown 内容发布为 Cloudflare Pages 静态站点。用户不需要维护 Git 仓库、命令行工具链或手写部署脚本，即可在 Obsidian 内完成建站、内容检查、预览和发布。

产品采用本地优先模型：

- Vault 是站点内容与可重建配置的来源。
- 扫描、解析、检查、渲染和预览优先在本地完成。
- Cloudflare 只承担身份授权、项目/域名管理和静态站点托管。
- 每次发布都生成完整站点，并通过新的 Cloudflare Pages 部署原子替换线上版本。
- 发布失败时，现有线上站点必须保持不变。

## 3. 问题陈述

希望公开 Obsidian 知识库的用户通常面临四类成本：

1. 需要理解静态站点生成器、Node.js、Git 和托管平台。
2. 很难在发布前确认哪些笔记、图片、链接和路径会暴露到公网。
3. 本地文件状态、发布意图和线上事实容易混淆，导致误发布、误下线或错误状态回写。
4. 现有工作流常把配置、缓存、凭据和内容散落在多个不可迁移位置，难以从 Vault 重建。

Pages Publish 要把这些成本收敛为一个可信流程：选择内容范围，审阅下一版站点，修复明确问题，预览，然后发布。

## 4. 目标用户

### 4.1 主要用户

- 使用 macOS Obsidian 桌面端维护个人知识库、教程、研究笔记或文档的作者。
- 希望公开其中一部分内容，但不想维护独立建站工程或部署流水线的用户。
- 已有或愿意创建 Cloudflare 账号，但不一定了解 Pages、API Token 或 DNS。

### 4.2 次要用户

- 熟悉 Cloudflare、Frontmatter 和静态站点的高级用户。
- 希望手动编辑可版本管理配置、绑定已有 Pages 项目或使用 API Token 的用户。

### 4.3 不针对的用户

- 需要多人实时 CMS、审稿流和角色权限的团队。
- 需要服务端动态逻辑、登录后内容或数据库驱动页面的站点。
- 需要 Windows、Linux、Obsidian Mobile 或多发布后端的用户。

## 5. 产品目标与成功判据

### 5.1 产品目标

1. 用户不打开终端即可完成首次建站、预览和发布。
2. 用户在发布前能看清新增、更新、URL 变化和待下线内容。
3. 私密内容和资源不会因链接、嵌入或目录配置而意外泄露。
4. Vault 加上可重建的站点配置足以重新生成完整站点。
5. Cloudflare 凭据、构建缓存和诊断数据不污染 Vault。
6. 任何失败都能说明线上影响与下一步，并支持安全重试。

### 5.2 首版成功判据

- 一位已有 Cloudflare 账号的目标用户可以在 Obsidian 内完成 OAuth、创建站点、选择内容、预览和首次发布。
- 在构建、上传或激活失败的测试中，原线上部署保持可访问且内容不变。
- 配置错误、缺失资源、私密图片和内容根异常都能在发布前被发现并定位。
- Vault、`site.yml`、构建产物、普通日志和诊断包中均不存在 Cloudflare 密钥。
- 删除、移动或改为私密的已上线文章在下一次成功发布后不再从原 URL 返回正文；已知 URL 变化按规则保留重定向。
- `public`、`unlisted`、`private` 在列表、搜索、图谱、站点地图和索引策略上的行为符合本规格。
- 明暗主题、键盘操作、窄工作区和 200% 缩放下，P0 流程仍可完成。

### 5.3 用户故事

1. 作为第一次发布知识库的 Obsidian 用户，我希望不使用终端即可创建站点，以便把精力放在内容上。
2. 作为没有合适 Node.js 环境的用户，我希望插件自动准备隔离的运行时，以免修改我的系统开发环境。
3. 作为已有兼容 Node.js 的用户，我希望插件安全复用它，以免重复下载运行时。
4. 作为 Cloudflare 用户，我希望通过 OAuth 登录，以免手动创建和复制 Token。
5. 作为高级用户，我希望可以使用最小权限 API Token，以便在 OAuth 不适合时继续使用。
6. 作为拥有多个 Cloudflare 账号的用户，我希望明确选择目标账号，以免把站点建在错误的位置。
7. 作为已有 Pages 项目的用户，我希望绑定兼容项目，以免创建重复站点。
8. 作为新用户，我希望在最终确认前看到将创建的本地与远端对象，以免向导提前产生副作用。
9. 作为站点作者，我希望配置多个内容根及其公开路径，以便只发布 Vault 中选定的区域。
10. 作为谨慎的作者，我希望内容根重叠和公开路径冲突被阻止，以免一篇文章发布到多个地址。
11. 作为站点作者，我希望首次设置后先审阅候选而不是立即发布，以免整批笔记意外上线。
12. 作为日常写作者，我希望新笔记默认私密，以免创建文件就自动暴露。
13. 作为作者，我希望看到下一版相对线上版本的新增、更新、URL 变化和待下线，以便一次审阅整站影响。
14. 作为作者，我希望发布列表中的选择明确代表下一版站点成员，以免误把它理解为“本次跳过更新”。
15. 作为作者，我希望下线已发布文章前看到确认，以免一次误点让线上页面消失。
16. 作为作者，我希望暂时缺失整个同步目录时发布被阻止，以免产生大规模意外下线。
17. 作为作者，我希望从当前文章旁边调整可见性和 URL，以便在写作上下文中管理发布意图。
18. 作为作者，我希望待发布值和当前线上值分开显示，以免把尚未部署的更改当成线上事实。
19. 作为作者，我希望修改已上线 URL 时自动保留旧地址重定向，以免外部链接失效。
20. 作为中文内容作者，我希望直接使用中文和 Unicode slug，以免被迫维护拼音地址。
21. 作为隐私敏感用户，我希望公开文章引用私密笔记时只保留我写的显示文本，以免泄露私密路径或标题。
22. 作为隐私敏感用户，我希望私密、被排除或缺失的本地图片阻塞发布，以免页面破损或资源意外公开。
23. 作为作者，我希望每个问题都能定位到源文件和行号，以便快速修复。
24. 作为作者，我希望 Warning 可以在理解影响后继续，而安全 Blocker 不能被绕过，以便平衡效率与安全。
25. 作为作者，我希望发布前用同一渲染链路预览文章或整站，以便看到接近线上结果的页面。
26. 作为作者，我希望发布期间继续编辑笔记时，本次部署仍使用开始时的稳定快照，以免线上产物前后不一致。
27. 作为已有线上读者的作者，我希望上传或激活失败时旧站点保持不变，以免一次失败造成停站。
28. 作为作者，我希望远端成功但本地状态回写失败时看到真实状态并能修复，以免重复发布或误判线上失败。
29. 作为希望迁移设备的用户，我希望站点可从 Vault 配置和内容重建，以免依赖不可见的私有数据库。
30. 作为安全敏感用户，我希望凭据只存 Keychain，且日志和诊断包自动脱敏，以免密钥或私密正文泄漏。
31. 作为 Obsidian 用户，我希望插件继承当前主题、键盘和侧栏习惯，以便它像原生工具一样工作。
32. 作为使用辅助技术的用户，我希望不依赖颜色且能用键盘完成核心流程，以便独立发布站点。
33. 作为高级用户，我希望可以直接编辑 `site.yml`，并在与设置页冲突时先比较或重载，以便保留可审查的配置工作流。
34. 作为站点维护者，我希望设置保存只更新本地规则和扫描结果，以免普通配置修改意外触发部署。
35. 作为站点维护者，我希望查看环境、缓存和脱敏诊断信息，以便在不暴露内容的情况下定位故障。
36. 作为站点读者，我希望公开页面可搜索并浏览知识图谱，而不列出页面不会出现在这些入口中，以符合作者的可见性选择。

## 6. 产品原则与硬性不变量

以下规则不能被设置项、快捷操作或错误恢复流程绕过：

1. **本地优先**：除 OAuth、远端管理、发布和用户主动外链检查外，核心内容处理不依赖网络。
2. **明确选择**：只有进入下一版站点的 Markdown 及其允许资源会被构建。
3. **意图与事实分离**：本地发布意图可以先于部署保存；线上事实只能在远端成功后更新。
4. **完整构建**：首版每次发布都从当前源快照构建完整站点，不实现单篇远端补丁。
5. **原子激活**：新部署只有完整上传并由 Cloudflare 成功激活后才算发布成功。
6. **失败不破坏线上**：构建、上传或激活失败不得覆盖现有线上版本。
7. **最小暴露**：未明确公开的笔记、路径、标题、URL、悬浮内容和资源不能出现在产物中。
8. **Blocker 不可忽略**：完整性或安全性 Blocker 没有“强制发布”入口。
9. **凭据隔离**：密钥只存在 macOS Keychain，不写入 Vault、插件设置、缓存、日志、产物或诊断包。
10. **远端删除隔离**：首版不提供删除 Cloudflare 项目或远端历史部署的能力。

## 7. 首版范围

### 7.1 包含

- 单个 Vault 对应单个 Cloudflare Pages 站点。
- macOS 桌面端环境探测与受管理的本地发布引擎。
- Cloudflare OAuth 主流程与 API Token 高级备用流程。
- 创建新 Pages 项目或绑定兼容的已有项目。
- `pages.dev` 域名与自定义域名连接/状态展示。
- 多个互不重叠的内容根与公开根路径映射。
- Markdown 内容扫描、Frontmatter 发布属性、链接与本地图片依赖分析。
- `public`、`unlisted`、`private` 三种可见性。
- 中文与 Unicode URL。
- 本地预览、整站完整构建、发布前报告和原子部署。
- 内置默认站点主题、目录栏目页、文章页、404、搜索和知识图谱。
- Obsidian Callout、代码块、Mermaid、常用 Markdown 与 Wiki 链接的首版兼容。
- 发布中心、首次设置、插件设置、当前文章面板和全局反馈入口。
- 最近一次发布结果、时间、站点链接与失败诊断。

### 7.2 不包含

- Windows、Linux、iOS、Android 和 Web 版 Obsidian。
- 多 Vault 合并站点、一个 Vault 多站点或团队协作 CMS。
- GitHub Pages、Netlify、Vercel 等其他发布后端。
- 单篇即时发布、局部远端补丁或发布队列。
- 插件内发布历史浏览、版本对比和回滚。
- 主题市场、自定义主题编辑器和第三方主题兼容承诺。
- 评论、访问统计、付费、登录、会员或服务端动态能力。
- 本地 PDF、音频和视频的直接托管。
- 图片压缩、响应式图片或 WebP/AVIF 自动转换。
- 多选文件右键批量操作。
- 删除 Cloudflare 项目。

## 8. 核心概念

| 概念 | 定义 |
| --- | --- |
| Vault | 当前 Obsidian 仓库，是内容与可重建配置的根边界 |
| 站点配置 | `.publish/site.yml` 中的站点级发布规则，是站点配置唯一事实来源 |
| 内容根 | 允许 Markdown 成为发布候选的 Vault 相对目录 |
| 公开根 | 某个内容根映射到站点的 URL 前缀 |
| 候选文章 | 位于有效内容根中的 Markdown 文件 |
| 发布意图 | 用户希望文章在下一版站点中的可见性、slug、标题覆盖等本地状态 |
| 部署事实 | 最近一次成功部署后确认的 URL、时间和内容摘要等只读事实 |
| 发布快照 | 用户确认发布时冻结的配置、候选内容和资源集合 |
| 扫描报告 | 当前源与最近成功部署之间的变化、问题和依赖结果 |
| Blocker | 为避免泄露、破损或不确定大规模下线而禁止发布的问题 |
| Warning | 允许发布，但用户应了解可能降级或性能影响的问题 |
| 下线 | 内容在下一次成功完整部署中不再出现；不代表删除本地文件 |
| 站点清单 | 最近成功部署的公开路径与摘要索引；属于可重建缓存，不是配置事实来源 |

## 9. 端到端用户流程

### 9.1 首次建站

1. 用户从 Ribbon 或命令面板进入首次设置。
2. 插件检查 macOS、Obsidian、Node.js 与本地发布引擎。
3. 插件复用兼容 Node.js；无兼容版本时准备插件受管理的运行时。
4. 用户填写站点名称和可选简介。
5. 用户配置一个或多个内容根及公开根。
6. 插件本地扫描，并展示候选数量、索引页和示例 URL。
7. 用户通过 Cloudflare OAuth 登录；高级用户可改用 API Token。
8. 用户选择账号并计划创建新项目或绑定已有项目。
9. 用户确认完整计划后，插件才写正式配置并执行远端变更。
10. 插件完成绑定和扫描后进入发布中心。
11. 此时没有文章发布，也没有文章 Frontmatter 被自动修改。

### 9.2 首次发布

1. 发布中心展示合格候选和建议的首次选择，但建议本身不写 Frontmatter。
2. 用户确认或调整哪些内容进入首版站点。
3. 用户触发发布时，插件先把用户确认的发布意图安全写入对应 Frontmatter。
4. 插件重新扫描并生成不可变发布快照。
5. 存在 Blocker 时停止；Warning 允许用户继续。
6. 用户可使用同一构建链路预览站点。
7. 用户确认发布后，插件依次执行准备、构建与检查、上传、激活。
8. 远端成功后，插件写回部署事实，并显示站点链接与结果。

### 9.3 日常更新

1. 插件监听相关 Vault 文件与配置变化，并进行防抖扫描。
2. 发布中心相对最近成功部署展示新增、更新、URL 变化、可见性变化和待下线。
3. 用户可从当前文章面板修改可见性、slug 和显式元数据覆盖。
4. 用户修复 Blocker，按需预览，然后从发布中心发布整站。
5. 发布中发生的后续 Vault 编辑不进入当前快照，而作为下一次待发布变化保留。

### 9.4 下线与恢复

1. 已上线文章被设为 `private`、从内容范围中明确移除、删除或移动出范围后，下一版站点将不再包含它。
2. 用户主动取消已上线文章时必须确认下线影响。
3. 内容根整体暂时缺失时，插件不得推断为用户要求批量下线，而是产生 Blocker。
4. 发布前恢复文件或内容根会撤销相应下线变化。

## 10. 功能需求

### FR-1 插件与本地环境

1. 插件仅在支持的 macOS Obsidian 桌面端声明完整可用。
2. 插件应探测可兼容的本机 Node.js，满足版本范围时优先复用。
3. 无兼容 Node.js 时，插件应下载并使用受管理运行时，不修改用户的系统 Node、npm、PATH 或全局包。
4. 发布引擎发行包必须来自固定可信来源，并验证版本与完整性校验值；有签名时同时验证签名。
5. 引擎更新失败时应保留最后一个已验证可用版本，并允许重试。
6. 环境准备、修复和更新必须显示真实阶段、失败原因与日志入口。
7. 本地扫描、构建和预览的长任务不得长期阻塞 Obsidian 主线程。

### FR-2 站点配置

1. Vault 根目录的 `.publish/site.yml` 是唯一站点级配置来源。
2. 设置页读取、校验并原子回写该文件，不维护第二份权威配置。
3. 用户在外部修改配置后，插件应自动重载；若设置页有未保存更改，则先展示冲突并禁止直接覆盖。
4. 配置保存成功后重新扫描，但不得自动预览或发布。
5. 无效 YAML、未知必填版本、无效路径或路由冲突必须阻塞发布并提供定位。
6. 配置写入使用临时文件、校验和原子替换；失败时旧配置继续有效。
7. 删除本地站点配置必须移到 Obsidian 回收站，不得删除 Cloudflare 项目或线上内容。

### FR-3 站点配置逻辑 schema

首版 `site.yml` 至少表达以下字段。字段名是产品契约，新增可选字段必须保持向后兼容。

| 字段 | 类型 | 必填 | 规则 |
| --- | --- | --- | --- |
| `version` | integer | 是 | 首版值为 `1`；不支持的未来版本只读并阻塞写入 |
| `site.name` | string | 是 | 非空，支持 Unicode |
| `site.description` | string | 否 | 最多 160 个用户可见字符 |
| `site.home_layout` | enum | 是 | `sections` 或 `latest`，默认 `sections` |
| `site.timezone` | IANA timezone | 否 | 缺省使用当前系统时区；保存后应固化以保证可重建 |
| `content_roots` | list | 是 | 至少一个，路径不可重叠 |
| `content_roots[].path` | Vault-relative path | 是 | 必须位于 Vault 内，不能含路径穿越 |
| `content_roots[].public_root` | URL path | 是 | 以 `/` 开始，规范化后彼此不能冲突 |
| `assets.exclude` | glob list | 否 | 明确禁止公开的 Vault 相对资源模式 |
| `features.search` | boolean | 是 | 默认 `true` |
| `features.graph` | boolean | 是 | 默认 `true` |
| `cloudflare.project_name` | string | 是 | 非密钥，用于迁移后重新绑定 |
| `cloudflare.custom_domain` | hostname | 否 | 仅表达期望/绑定域名，不保存凭据 |

配置约束：

- 内容根不能相同、互相包含或经路径规范化后重叠。
- 公开根不能相同，也不能使两篇候选内容生成同一路由。
- Vault 根可以作为内容根，但首次选择和保存时都要提示暴露范围。
- 符号链接不在首版扫描范围内；不得跟随符号链接读取 Vault 外内容。
- `assets.exclude` 命中优先于“被公开文章引用”。
- Cloudflare 账号标识、OAuth refresh token、API Token 和临时授权信息不属于 `site.yml`。

### FR-4 本地存储分层

| 数据 | 存储位置 | 权威性 | 清理后影响 |
| --- | --- | --- | --- |
| 站点规则 | `.publish/site.yml` | 权威配置 | 无配置则不能发布 |
| 文章意图与部署事实 | Markdown Frontmatter 的 `publication` | 权威文章状态 | 删除文章会随完整构建下线 |
| Cloudflare 密钥 | macOS Keychain | 权威凭据 | 需重新授权，不影响 Vault |
| 非密钥设备绑定 | Obsidian 插件本地数据 | 可恢复 | 可通过登录和项目名重新绑定 |
| 引擎、构建与扫描缓存 | macOS 缓存目录 | 非权威 | 自动重建 |
| 最近部署清单 | 本地缓存 | 非权威 | 失去精细 diff，但不影响完整构建正确性 |
| 临时日志与恢复收据 | macOS 应用支持/缓存目录 | 临时恢复依据 | 按保留策略清理 |

任何不可重建本地数据都不得成为生成正确站点的必要条件。

### FR-5 Cloudflare 授权与绑定

1. OAuth 是默认路径，只请求 Pages 项目管理、部署和读取状态所需的最小权限。
2. API Token 是高级备用路径；界面必须说明所需权限并在保存前验证。
3. 凭据只写入 Keychain，UI 只显示账号身份与连接状态。
4. 用户可以选择账号、创建新项目或绑定已有项目。
5. 最终确认前，向导不得创建项目、绑定域名或写正式远端状态。
6. 创建/绑定操作必须幂等；重试优先复用已创建且与当前计划匹配的项目。
7. 绑定已有项目前必须验证账号归属和 Pages 兼容性。
8. 账号切换、重新授权、项目重绑和域名操作与普通设置保存隔离。
9. 远端动作失败时，现有本地绑定保持不变。
10. 用户可使用 `pages.dev` 域名或连接自定义域名，并查看待验证、有效、失败状态。
11. 首版不提供删除项目、删除部署历史或强制接管不兼容项目。

### FR-6 内容发现与扫描

1. 只有有效内容根内的 Markdown 文件可以成为文章候选。
2. 非 Markdown 文件不因位于内容根中而自动发布。
3. 被下一版公开或不列出的 Markdown 直接引用、存在于 Vault 内且通过检查的图片可以进入构建。
4. 扫描在插件加载、相关文件变化、配置保存、用户手动刷新、预览前和发布前触发。
5. 自动扫描应防抖并可取消过时任务；新结果不得被旧任务覆盖。
6. 扫描默认不请求外部 URL，只进行语法、路径、引用和本地文件检查。
7. 扫描不得修改 Frontmatter、远端项目或线上部署。
8. 扫描结果至少包含候选、有效可见性、待发布 URL、当前线上 URL、变化类型、依赖、Blocker 和 Warning。
9. 相同输入、配置和引擎版本必须产生相同扫描结果。
10. 内容根整体缺失、不可读或疑似未挂载时产生防批量下线 Blocker。

### FR-7 文章发布元数据

文章级状态统一放在 Frontmatter 的 `publication` 对象。插件可读取旧字段并提供无损迁移预览，但首版只写新 schema。

#### 7.1 用户可编辑的发布意图

| 字段 | 类型 | 缺省/回退 | 规则 |
| --- | --- | --- | --- |
| `publication.visibility` | enum | `private` | `public`、`unlisted`、`private` |
| `publication.title` | string | 首个 H1，再回退文件名 | 仅显式覆盖时写入 |
| `publication.summary` | string | 按正文摘要规则生成 | 仅显式覆盖时写入 |
| `publication.slug` | string | 文件名规范化结果 | 支持 Unicode；不能改变路径层级 |
| `publication.date` | local date/time | 通用 `date` 属性，再回退首次成功发布时间 | 用户手填优先 |
| `publication.updated` | local date/time | 最近一次内容成功发布时生成 | 用户手填优先 |
| `publication.tags` | string list | 文章现有标签 | 仅显式覆盖时写入 |
| `publication.cover` | Vault-relative path | 无 | 必须通过公开资源检查 |
| `publication.kind` | enum | `article` | `article` 或 `index` |
| `publication.order` | number | 无 | 有值时优先用于栏目固定排序 |
| `publication.redirects` | URL path list | 空 | 规范化、去重，不得与现有路由冲突 |

#### 7.2 系统管理的部署事实

`publication.deployment` 为只读系统区，至少包含：

| 字段 | 作用 |
| --- | --- |
| `url` | 最近一次成功部署中的主 URL |
| `first_published_at` | 首次成功上线时间 |
| `last_published_at` | 最近一次成功上线时间 |
| `source_digest` | 最近成功部署所用源快照摘要，用于变化判断 |
| `deployment_id` | 对应 Cloudflare 部署标识，用于诊断和恢复 |

部署事实规则：

- 只有远端激活成功后才能写入。
- 构建、上传或激活失败不得更新。
- 如果远端已经成功而本地 Frontmatter 回写失败，发布必须显示“线上成功、本地同步待修复”，并通过本地恢复收据重试回写；不能把它误报为线上失败。
- 回写完成前阻止新的发布，但允许打开站点、导出诊断和执行修复。
- 动态标题、摘要等回退值不因扫描自动固化。

### FR-8 可见性

| 行为 | `public` | `unlisted` | `private` |
| --- | --- | --- | --- |
| 生成可访问页面 | 是 | 是 | 否 |
| 出现在首页/栏目列表 | 是 | 否 | 否 |
| 进入站内搜索 | 是 | 否 | 否 |
| 进入知识图谱 | 是 | 否 | 否 |
| 进入站点地图 | 是 | 否 | 否 |
| 搜索引擎索引 | 允许 | `noindex` | 无页面 |
| 知道 URL 可访问 | 是 | 是 | 否 |

- 界面必须明确说明 `unlisted` 不是访问控制或保密机制。
- 新文章缺少显式可见性时按 `private` 处理。
- 首次发布建议选择只是 UI 草稿；用户确认发布时才形成显式本地意图。
- 日常在文章面板或发布中心明确修改可见性时立即写入 Frontmatter；这不代表线上已经改变。
- 已上线文章变为 `private` 时形成待下线状态并要求确认。

### FR-9 URL 与目录映射

1. 文章 URL 由公开根、相对目录和显式或派生 slug 组成。
2. slug 支持中文和其他 Unicode 字符，不自动转拼音。
3. slug 禁止 `/`、路径穿越、查询串和 fragment 控制字符。
4. 所有生成路由、重定向与系统页面路径必须全站唯一。
5. 用户通过 UI 修改已上线文章 URL 时，旧 URL 自动加入 redirects。
6. 用户直接修改 Frontmatter 且插件无法可靠识别旧 URL 时产生 Warning，不猜测历史地址。
7. 已知旧 URL 在后续完整部署中输出永久重定向；重定向链应压平到当前 URL。
8. 重定向循环、目标缺失或与页面路由冲突是 Blocker。
9. 目录结构映射为站点栏目结构。
10. `_index.md` 优先作为目录索引内容；没有时使用 `index.md`；两者同时存在时使用 `_index.md`，不产生额外 Warning，且 `index.md` 不作为同目录的第二个索引页面发布。
11. 没有索引文件时，插件按目录名和默认说明生成栏目页。

### FR-10 链接、嵌入与资源

#### 10.1 Markdown 与笔记引用

1. 指向下一版 `public` 或 `unlisted` 文章的内部链接解析为目标线上 URL。
2. 指向 `private`、范围外或不存在文章的 Wiki 链接降级为不可点击显示文本。
3. 降级结果不得输出目标文件路径、推导标题、URL、悬浮预览或反向链接。
4. 指向私密文章的 Markdown 嵌入按同一规则降级为普通显示文本，不嵌入正文。
5. 指向不存在文章的链接产生 Warning；降级后允许发布。
6. 公开文章之间的循环普通链接允许；循环嵌入必须被检测并阻止无限递归。

#### 10.2 本地图片

首版支持 PNG、JPEG、WebP、GIF 和 SVG。

图片只有同时满足以下条件才进入构建：

- 被下一版 `public` 或 `unlisted` 文章直接引用。
- 位于当前 Vault 内且不是符号链接逃逸目标。
- 未命中 `assets.exclude`。
- 文件存在、可读且格式受支持。
- SVG 不包含脚本、外部加载或其他活动内容；不安全 SVG 是 Blocker。

缺失、范围外、被排除或不安全的图片是 Blocker，因为继续发布会造成页面破损或隐私风险。

图片首版原样复制。超过 5 MiB 时产生性能 Warning，但不阻塞；不进行压缩或格式转换。

#### 10.3 其他附件与外部资源

- 本地 PDF、音频、视频和其他附件首版不上传；引用降级为显示文本并产生 Warning。
- 外部 HTTP(S) 图片和链接保持外链，构建器不下载、不代理。
- 外链默认只检查 URL 语法。用户主动运行“检查外链”时才请求网络并展示临时结果。
- 外链检查失败是 Warning，不阻塞核心发布。

### FR-11 Markdown 渲染与站点输出

1. 内置发布引擎必须支持 CommonMark/GFM 基础表达、代码块、表格、任务列表、Obsidian Callout、Mermaid 和受支持 Wiki 链接。
2. 不支持或发生降级的 Obsidian 专有语法应在扫描报告中产生 Warning，并允许用户定位和预览。
3. 原始 HTML 必须经过安全清理；脚本、事件处理器和危险 URL 不得进入产物。
4. 代码块保持语言标记；Mermaid 在受控客户端运行时渲染，不允许注入任意脚本。
5. 内置默认主题必须生成首页、栏目页、文章页、搜索页、图谱页、404 和基础隐私说明。
6. 首页布局支持按栏目和按最新文章两种模式。
7. 栏目默认按发布日期倒序；`publication.order` 存在时按固定顺序优先，再使用默认排序。
8. public 页面生成 canonical、站点地图条目和必要的基础 metadata。
9. unlisted 页面生成 `noindex`，不出现在导航、搜索索引、图谱和站点地图中。
10. 站点构建不得包含 private 文章的正文、标题、路径、标签、关系或资源。

### FR-12 变化检测与发布报告

扫描结果相对最近一次成功部署分类：

- 新增。
- 正文或有效元数据更新。
- 可见性变化。
- URL 变化。
- 待下线。
- 无变化。
- 状态未知（最近部署清单不可用）。

要求：

1. 摘要展示总变化、新增、更新、待下线、Blocker 和 Warning。
2. 每个问题包含级别、原因、线上影响、源文件、精确行号和定位动作。
3. Warning 允许发布；Blocker 禁用发布且没有忽略开关。
4. 最近部署清单丢失时仍允许安全完整构建，但必须说明精细 diff 不可用，并在发布前显示本次完整输出规模。
5. Checkbox 表示文章是否进入下一版完整站点，不表示“跳过本次更新”。
6. 取消已上线文章必须展示待下线确认。

### FR-13 预览

1. 预览使用与发布相同的解析、检查、路由和渲染链路。
2. 本地预览不需要 Cloudflare 授权或网络。
3. 用户可以预览当前文章或完整站点。
4. 预览必须明显标识为本地结果，并显示当前 Blocker/Warning；预览成功不代表允许发布。
5. 能安全降级的问题可在预览中显示占位与说明；无法生成确定输出的致命构建错误阻止预览。
6. 预览服务可以关闭和重启，不成为发布前强制步骤。

### FR-14 发布编排

1. 只有发布中心可以发起发布。
2. 发布前必须重新验证配置、授权、内容根和当前发布意图。
3. 用户确认后生成不可变发布快照；发布期间的新编辑进入下一次变化。
4. 阶段固定为：准备 → 构建与检查 → 上传 → 激活。
5. 只显示真实阶段，不显示推测百分比。
6. 构建与上传前发现 Blocker 时停止且不修改远端。
7. 上传开始后不提供无法保证的取消；关闭界面不终止后台任务。
8. Cloudflare 新部署成功激活前，旧部署持续提供服务。
9. 构建、上传或激活失败时不更新部署事实，保留本地发布意图和可重试诊断。
10. 重试前重新扫描并生成新快照，不盲目复用可能过期的构建目录。
11. 发布成功后更新部署事实、最近部署清单和 UI 状态。
12. 若远端成功但本地事实回写不完整，进入“待协调”状态并按 FR-7 的恢复规则处理。

### FR-15 日期与时区

1. 站点使用 `site.timezone`；首次保存配置时缺省为当前系统 IANA 时区。
2. 用户显式填写的 `publication.date` 和 `publication.updated` 优先，不自动覆盖。
3. 首次成功发布且没有任何日期来源时，以站点时区的成功时间作为有效发布日期，并固化到 Frontmatter。
4. 内容发生实际更新并成功发布、且用户没有显式管理 updated 时，插件更新有效修改时间。
5. 扫描或失败发布不得更新日期或成功时间。

### FR-16 界面与入口

1. 发布中心、首次设置、设置页、当前文章面板和全局入口按 `UI-SPEC.MD` 实现。
2. 发布中心位于主工作区，是唯一发布入口。
3. 首次设置位于主工作区，最终确认前不执行远端修改。
4. 设置位于 Obsidian 原生插件设置页，本地设置保存与远端操作隔离。
5. 当前文章面板位于右侧边栏，跟随活动 Markdown，可固定，但不提供单篇发布。
6. 使用一个 Ribbon 图标；命令面板和 Markdown 右键菜单提供安全的上下文入口。
7. 状态栏正常空闲时隐藏，只在扫描、待发布、Blocker、发布中或失败时出现。
8. Notice 只用于用户主动动作结果、授权/配置重要变化和后台发布完成，不为常规扫描制造噪音。
9. UI 必须始终区分待发布值和当前线上值。
10. 视觉、响应式、动效、文案和可访问性遵循 `DESIGN.md`。

### FR-17 设置与维护

1. 用户可修改站点名称、简介、首页布局、内容范围、搜索、图谱和 Cloudflare 绑定。
2. 修改公开根前展示受影响 URL 数与自动重定向说明。
3. 移除内容根前展示已上线文章的待下线数量。
4. 用户可查看环境版本、预览服务、缓存和日志状态。
5. 用户可修复本地环境、清理可重建缓存、打开日志和导出诊断包。
6. 远端项目删除、强制清空线上内容和凭据明文查看不作为维护动作提供。

### FR-18 日志、诊断与隐私

1. 日志记录时间、阶段、错误码、引擎版本、非敏感计数和可安全暴露的相对定位。
2. 普通日志不记录 OAuth/API Token、Authorization header、文章正文、私密文章标题/路径或资源内容。
3. 对本地路径进行最小披露；UI 定位需要完整路径时只在本机交互中显示。
4. 诊断包默认包含版本、平台、脱敏配置结构、状态、错误和关键日志，不含凭据、文章正文或构建产物。
5. 导出前展示包含/排除项，用户明确确认后才写出文件。
6. 临时日志、构建目录和恢复收据采用有界保留策略；恢复收据完成协调后尽快删除。

## 11. 状态模型

### 11.1 站点状态

| 状态 | 允许操作 | 禁止操作 |
| --- | --- | --- |
| 未配置 | 开始设置、检查环境 | 扫描、预览、发布 |
| 环境准备中 | 查看详情 | 重复准备、发布 |
| 设置草稿 | 编辑向导、退出并保留草稿 | 发布、未确认远端修改 |
| 配置无效 | 打开/修复配置、诊断 | 预览、发布 |
| 需授权 | 浏览本地内容、重新授权 | 远端操作、发布 |
| 扫描中 | 查看旧结果、等待/重新触发最新扫描 | 基于过时结果发布 |
| 就绪 | 审阅、预览、发布 | — |
| 有 Blocker | 定位、修复、预览可降级部分 | 发布 |
| 发布中 | 查看阶段和日志、关闭视图 | 开始第二次发布、修改远端绑定 |
| 发布成功 | 打开站点、查看结果 | — |
| 发布失败 | 查看详情、修复、重试 | 把失败当成功回写 |
| 线上成功/本地待协调 | 打开站点、修复本地状态、导出诊断 | 新发布、覆盖恢复收据 |

### 11.2 文章状态

文章可以同时具有一个部署状态和零个或多个问题：

| 部署状态 | 判定 |
| --- | --- |
| 未发布 | 没有成功部署事实，且下一版不包含 |
| 待首次发布 | 下一版包含，但没有成功部署事实 |
| 已同步 | 当前有效摘要和 URL 与最近成功部署一致 |
| 有更新 | 内容或有效元数据摘要变化 |
| 可见性待更新 | 发布意图与线上可见性不同 |
| URL 待更新 | 下一版 URL 与线上 URL 不同 |
| 待下线 | 线上存在但下一版不包含 |
| 状态未知 | 缺少可比较的最近部署事实或清单 |

问题级别独立为 Passed、Info、Warning、Blocker。上次发布失败属于站点/任务结果，不覆盖文章本身的部署状态。

## 12. 深模块与产品接口边界

实现应优先形成以下可独立测试的深模块。这里定义职责，不限定具体文件结构。

| 模块 | 简单入口 | 封装职责 |
| --- | --- | --- |
| 配置仓储 | load / validate / save | YAML schema、冲突检测、外部编辑协调、原子写入 |
| 文章元数据仓储 | read / planWrite / commitFacts | Frontmatter 回退、意图写入、部署事实、旧字段迁移 |
| 内容索引器 | scan(snapshotInput) | 内容根、候选、文件事件、防抖、稳定摘要 |
| 路由规划器 | plan(site, articles) | URL、栏目、Unicode slug、冲突、redirects |
| 依赖与隐私检查器 | inspect(graph) | 笔记链接、嵌入、图片、排除规则、泄露防护 |
| 站点构建器 | build(snapshot) | Markdown 转换、主题、搜索/图谱索引、完整输出 |
| 环境管理器 | ensure / repair | Node 复用、受管理运行时、引擎校验与回退 |
| Cloudflare 适配器 | authorize / bind / deploy / inspect | 最小权限、项目、域名、部署与远端状态 |
| 发布编排器 | prepare / publish / reconcile | 不可变快照、阶段、幂等、失败语义、成功后协调 |
| 预览服务 | start / stop / open | 复用构建产物、本地服务与生命周期 |
| 诊断服务 | record / export | 脱敏日志、错误映射、恢复收据与导出 |
| UI 状态投影 | project(domainState) | 将统一领域状态投影到四个界面和全局反馈入口 |

Cloudflare 适配器是首个后端实现；内部边界应允许未来适配其他静态托管，但首版 UI 和配置不暴露多后端概念。

## 13. 非功能需求

### 13.1 可靠性

- 所有本地配置与 Frontmatter 写入必须先校验并使用安全替换策略。
- 远端创建、绑定、部署和本地协调操作必须支持幂等重试。
- 过时扫描和构建结果不能覆盖较新的领域状态。
- 关闭发布中心或重启 Obsidian 后，进行中/待协调任务必须可被识别和恢复说明。

### 13.2 安全

- 所有路径在读取和输出前规范化并验证 Vault 边界。
- 不跟随可能逃逸 Vault 的符号链接。
- HTML、SVG、Mermaid 与 URL 输出经过对应安全策略。
- 仅公开构建需要的数据，禁止把完整 Vault 索引打包进站点。
- Cloudflare 权限最小化，Token 永不回显。

### 13.3 性能与响应

- 扫描、哈希、构建和上传在后台执行，UI 保持可操作。
- 文件变化采用增量索引优化用户等待，但发布正确性不依赖缓存。
- 长任务显示阶段、当前对象或计数；没有真实依据时不展示百分比。
- 首版在确定发布前应建立大 Vault 基准，并据此固化定量性能门槛。

### 13.4 可访问性与兼容性

- P0 流程可仅用键盘完成，焦点顺序和状态播报符合 `DESIGN.md`。
- 明暗主题均不依赖硬编码颜色。
- 容器小于 640px 时不产生页面级横向滚动。
- 中文文件名、Unicode slug、长路径、混合中英文和 200% 缩放可用。
- 发布前必须明确最低 Obsidian 版本，并在 manifest 与发布说明中一致声明。

### 13.5 可观察性

- 每次扫描和发布有本地任务标识、开始/结束时间、阶段和结构化结果。
- 用户可从失败界面进入相关日志，而无需在文件系统中寻找。
- 错误文案同时表达发生了什么、线上影响和下一步。

## 14. 测试决策

测试只验证外部行为和领域不变量，不绑定内部类名、私有函数或 UI DOM 偶然结构。

### 14.1 必须自动化的模块

1. **配置仓储**：schema、路径规范化、内容根重叠、公开路由冲突、外部编辑冲突和原子保存失败。
2. **文章元数据**：回退优先级、显式意图、旧字段迁移预览、成功/失败后的部署事实写入。
3. **路由规划器**：Unicode、索引页优先级、slug 冲突、重定向压平、循环与路径穿越。
4. **依赖与隐私检查器**：public/unlisted/private 组合、私密链接降级、私密图片 Blocker、循环嵌入和 SVG 安全。
5. **站点构建器**：固定 fixture Vault 的页面集合、搜索/图谱收录、无私密内容泄漏和确定性输出。
6. **发布编排器**：四阶段、并发保护、Vault 中途变化、各阶段失败、远端成功/本地回写失败和幂等协调。
7. **凭据与诊断**：Keychain 接口契约、日志/诊断包脱敏和 Token 不落盘。

### 14.2 集成测试

- 使用临时 fixture Vault 覆盖首次发布、日常更新、移动、删除、私密化、内容根缺失与恢复。
- 使用 fake Cloudflare adapter 验证远端调用顺序和失败语义。
- 使用真实发布引擎构建 fixture，并对页面、路由、重定向和索引产物做语义断言。
- Cloudflare 真实账号测试只在隔离的人工/受控环境运行，不要求普通 CI 持有生产凭据。

### 14.3 UI 与人工验收

- 四个核心界面覆盖 `UI-SPEC.MD` 中的 P0/P1/P2 状态。
- 在 Obsidian 默认明暗主题、窄侧栏、分屏和 200% 缩放下做视觉验收。
- 用键盘完成首次设置、定位 Blocker、预览、发布和失败重试。
- 验证设置保存、扫描、预览和发布不会被错误串联。
- 验证 Notice 与状态栏在正常扫描期间保持低噪音。

### 14.4 回归底线

任何实现不得在缺少以下测试的情况下交付：

- 发布失败后线上站点保持不变。
- private 内容不进入任何公开产物或索引。
- 内容根暂时缺失不会触发无确认批量下线。
- URL 变化生成正确重定向且无循环。
- 远端成功、本地回写失败可以恢复。
- 凭据不进入 Vault、日志、构建产物或诊断包。

当前仓库没有既有代码或测试先例。实现阶段应以本节定义的外部行为建立第一套测试结构。

## 15. 验收标准

### AC-1 首次设置

- Given 当前 Vault 尚未配置，When 用户完成环境准备和四步向导，Then 插件创建/绑定一个 Pages 项目、写入有效 `site.yml` 并进入发布中心。
- Given 用户尚未确认最后一步，When 在前几步编辑配置，Then 不创建远端项目、不绑定域名、不修改文章 Frontmatter。
- Given 创建项目后本地步骤失败，When 用户重试，Then 复用匹配项目且不会创建重复项目。

### AC-2 内容范围

- Given 两个内容根重叠或公开根冲突，When 用户验证或保存，Then 保存失败并定位冲突项。
- Given 一个已配置内容根整体缺失，When 插件扫描，Then 发布被阻塞且不会把该根全部内容直接判定为确认下线。
- Given Vault 中存在范围外 Markdown，When 扫描，Then 它不会成为候选，也不会泄露标题或路径到产物。

### AC-3 可见性与隐私

- Given public、unlisted、private 各一篇文章，When 构建，Then public 被导航/搜索/图谱/站点地图收录，unlisted 仅可直链且 noindex，private 没有页面和索引记录。
- Given public 文章链接 private 文章，When 构建，Then 只保留作者显示文本，不输出目标路径、标题或 URL。
- Given public 文章引用被排除或缺失图片，When 发布前检查，Then 产生不可忽略 Blocker。

### AC-4 URL 与下线

- Given 已上线文章通过 UI 修改 slug，When 下一次发布成功，Then 新 URL 返回文章且旧 URL 永久重定向到新 URL。
- Given 已上线文章被删除、明确移出内容范围或改为 private，When 下一次完整发布成功，Then 旧页面不再提供正文。
- Given 用户取消已上线文章，When 尚未确认，Then 本地意图与线上页面都不改变。

### AC-5 预览与发布

- Given Cloudflare 未授权但本地配置有效，When 用户预览，Then 本地预览可用而发布不可用。
- Given 存在 Warning 且无 Blocker，When 用户确认发布，Then 发布可以继续并保留 Warning 报告。
- Given 存在 Blocker，When 用户查看发布中心，Then 发布按钮禁用并展示原因和定位入口。
- Given 用户开始发布后继续编辑 Vault，When 当前发布成功，Then 线上内容来自开始时快照，后续编辑显示为下一次变化。

### AC-6 失败与恢复

- Given 构建、上传或激活任一阶段失败，When 任务结束，Then 现有线上站点和部署事实保持不变，并可从失败原因重试。
- Given Cloudflare 已激活新部署但 Frontmatter 回写失败，When 插件恢复或重启，Then 显示线上成功/本地待协调并可幂等完成回写。
- Given OAuth 失效，When 用户尝试发布，Then 发布暂停、文章本地编辑仍可用并提供重新授权入口。

### AC-7 配置与凭据

- Given 设置页有未保存更改且 `site.yml` 被外部修改，When 插件检测到冲突，Then 禁止静默覆盖并提供重载/比较选择。
- Given 用户保存普通设置，When 保存完成，Then 配置被原子写入并重新扫描，但不会创建部署。
- Given 用户导出诊断包，When 检查包内容，Then 不存在凭据、Authorization header、文章正文和私密资源内容。

### AC-8 UI 与可访问性

- Given Obsidian 明暗主题和不同容器宽度，When 用户进入四个核心界面，Then 信息层级、状态和 Primary 行为符合 UI 文档。
- Given 用户仅使用键盘，When 完成首次设置、修复问题、预览并发布，Then 所有功能可达且焦点可见。
- Given 插件空闲且没有待处理状态，When 用户正常编辑笔记，Then 状态栏隐藏且不产生扫描 Notice。

## 16. 发布门槛

首个公开版本发布前必须完成：

- [ ] 明确并验证最低 Obsidian 版本与兼容 Node 范围。
- [ ] 固化 `site.yml` 与 `publication` schema v1，并提供旧字段迁移 fixture。
- [ ] 完成所有 P0 自动化测试与 AC-1 至 AC-7 的可重复验证。
- [ ] 在隔离 Cloudflare 账号完成创建、绑定、部署失败和自定义域名状态验收。
- [ ] 对至少一个大 Vault fixture 建立扫描、构建、内存和 UI 响应基准。
- [ ] 完成明暗主题、窄容器、键盘和 200% 缩放的人类验收。
- [ ] 检查构建产物、日志、缓存和诊断包的私密信息泄漏。
- [ ] 验证引擎包来源、校验、回退和离线失败文案。

## 17. 已知风险与后续方向

### 17.1 首版风险

- Obsidian Markdown 与插件生态语法繁多，首版只能明确支持核心集合；降级预览和定位必须可靠。
- 大 Vault 的文件监听、哈希和依赖图可能影响 Obsidian 响应，需要基准后设置性能门槛。
- Cloudflare OAuth、项目绑定与自定义域名能力会受到 API 和权限模型变化影响，需要适配器隔离。
- Frontmatter 多文件回写无法形成真正跨文件原子事务，因此必须依靠远端成功后的恢复收据与幂等协调。
- SVG、原始 HTML 和 Mermaid 都具有内容安全风险，安全策略不可为了“兼容”而关闭。

### 17.2 后续候选

- 图片压缩、响应式尺寸和现代格式转换。
- 评论适配器，优先考虑 Giscus 或自托管方案。
- Cloudflare Web Analytics 与隐私说明联动。
- 发布历史、版本对比、回滚和站点快照导出。
- 多站点、多后端和 GitHub Pages 等发布适配器。
- 主题扩展、主题市场与更完整的 Obsidian 语法兼容。
- Windows/Linux 桌面端与 Obsidian Mobile 能力评估。

## Source Manifest

### Sources

- [Obsidian 发布插件架构](thread://019fb7b2-bf37-7883-9334-53dac3a35ca1)：本地优先架构、Cloudflare、内容模型、资源边界、可见性、完整构建、原子部署、失败语义与首版默认决策。已通过 `read_thread` 重读。
- [规划_插件UI设计_2026-07-31](thread://019fb857-2600-7353-9f31-a9519017a003)：四个核心界面、入口反馈系统、状态与设计规范的确认过程。
- [`DESIGN.md`](./DESIGN.md)：已确认的全局视觉、组件、反馈、安全不变量和可访问性规范。
- [`UI-SPEC.MD`](./UI-SPEC.MD)：已确认的 ASCII 原型、页面交互、跨界面状态和首版 UI 非目标。
- 用户明确指令（2026-07-31）：UI 原型确定后生成最终产品 spec 文件。
- `/Users/ivan/.agents/docs/agents/workflows.md` 与 `/Users/ivan/.agents/docs/agents/handoff-policy.md`：持久化产品规格与 Source Manifest 要求。
- `/Users/ivan/.agents/skills/to-prd/SKILL.md`：问题、方案、用户故事/需求、实现决策、测试与非目标的规格完整性检查方法；按用户要求仅生成本地文件，未发布 issue。

### Produced artifacts

- `/Users/ivan/workspace/ai/obsidian-pages-plugin/PRODUCT-SPEC.md`

### Key decisions

- 首版是 macOS 单 Vault、单 Cloudflare Pages 站点、本地优先的 Obsidian 插件。
- `.publish/site.yml` 是唯一站点配置源，`publication` 是文章意图与部署事实的统一 Frontmatter 对象。
- 公开内容采用三态可见性；unlisted 可直链但不收录，private 不进入任何公开产物。
- 发布采用不可变快照、完整构建和原子激活；失败不改变线上，远端成功/本地回写失败进入可恢复协调状态。
- UI 只保留一个整站发布入口；设置、扫描、预览和远端操作相互隔离。
- 首版内置默认主题，开启搜索和图谱，不包含评论、统计、历史回滚和多后端。

### Verification evidence

- 规格与 `DESIGN.md`、`UI-SPEC.MD` 以及架构线程的已确认决策逐项对照。
- 使用 `git diff --check` 检查 Markdown 空白错误，并检查标题结构、需求 ID、验收 ID 和 Source Manifest 完整性。
- 当前仓库没有实现代码，因此未执行构建、测试、Cloudflare API、Obsidian UI 或 HAT 验证。

### Open questions / risks

- 最低 Obsidian 版本、兼容 Node 版本范围和发布引擎发行机制需在实现启动时依据真实依赖固化。
- Cloudflare OAuth 的最终权限、回调机制和自定义域名 API 需要实现期以官方 API 验证。
- 5 MiB 图片 Warning 阈值和大 Vault 性能门槛属于首版默认值，需用 fixture 基准校准。
- 用户若希望把本规格发布成正式 PRD issue，下一步可使用 `to-prd`；若直接研发，可先使用 `to-issues` 拆分垂直切片。
