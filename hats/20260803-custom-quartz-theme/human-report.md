# HAT Copilot Report

## Scope

- Source: [`guide.md`](./guide.md)
- Candidate branch: `main`; base feature commit: `25318e6 feat: add external Quartz theme platform`
- Scope: external Quartz theme installation/trust, Brutalist UI preview, recovery and accessibility acceptance.
- Acceptance judgment belongs to the human tester.

## Progress

- [x] Environment
- [x] Data and local theme artifact
- [ ] P0 acceptance
- [ ] P1 acceptance
- [ ] Final summary

## Environment

- Vault: [`test-vault/`](./test-vault/)
- Plugin: `.obsidian/plugins/pages-publish/`, enabled through `.obsidian/community-plugins.json`
- Candidate: `release/pages-publish-0.1.0/`, exactly `main.js`, `manifest.json`, `styles.css`
- Theme: local immutable `.tgz` under `.publish/themes/`
- Prepare command: `hats/20260803-custom-quartz-theme/prepare.sh prepare`
- Status: ready for human acceptance; reload Obsidian after every candidate update.
- Rich-content baseline: 20 Markdown notes and 3 local SVG assets, including four-level nesting, short/medium/long articles, mixed Chinese/English/Unicode, tables, tasks, callouts, code, math, Mermaid, footnotes and image-text layouts.
- LAN preview: `http://192.168.2.95:4188/` (same Wi-Fi; local process only).

## Acceptance Cases

### P0

- [x] HAT-P0-001 — 三文件插件与外部主题隔离
  - Status: PASS (automated)
  - Human result: not required
  - Notes: candidate and Vault plugin copies are byte-identical; release contains exactly three files.
  - Next: HAT-P0-002

- [ ] HAT-P0-002 — 本地 `.tgz` 导入、身份和显式信任
  - Status: RETEST READY
  - Human result: two issues found during the installation preconditions.
  - Evidence: [`preinstall fail-closed`](./human-artifacts/HAT-P0-002-preinstall-fail-closed.png), [`pathless File import failure`](./human-artifacts/HAT-P0-002-pathless-file.png)
  - Notes: the fixture now starts on Quartz default. The second failure was a product bug: Obsidian 1.13.4 returned a standards-compliant pathless `File`, while the UI required non-standard `File.path`. The fixed candidate reads bounded `.tgz` bytes through `File.arrayBuffer()` and passes them through the existing integrity/copy/smoke pipeline.
  - Verification: pathless selection + installer regression tests pass; full suite is 675 passed / 8 skipped; Test Vault plugin is byte-identical to the rebuilt release.
  - Next: reload Obsidian and select the same `.tgz`; the executable-theme trust warning should replace the path error.

- [x] HAT-P0-003 — 真实 Quartz 深度主题构建
  - Status: PASS (automated)
  - Human result: visual confirmation pending
  - Notes: real Quartz integration and deterministic repeated output passed.
  - Next: confirm poster/editorial/instrument visual language in preview.

- [x] HAT-P0-004 — 可见性、路由、CSP 与离线资源
  - Status: PASS (automated)
  - Human result: physical offline refresh optional
  - Notes: private/unlisted/discovery/CSP/local-resource assertions passed.

### P1

- [ ] HAT-P1-001 — 响应式、暗色、搜索、图谱和长文工具
  - Status: AUTOMATED PASS / HUMAN REVIEW READY
  - Human result: pending
  - Notes: the real Quartz 5 builder emitted 97 files and 2 generated assets from 20 public/unlisted candidates with zero scan warnings. Chrome at 820×1180 verified the long-form page remains at `scrollY = 0` after Explorer hydration. The production shell follow-up rechecked 1440×1000 focused reading, 1024×768 compact navigation, 820×1180 and 390×844 drawers, mobile full-screen Search and image preview. At 1440×1000 it also opened Quartz's native knowledge graph as a labelled modal, verified its compact high-contrast title bar, inert background and no horizontal overflow, then closed it without a native-trigger re-open. A final 320×844 review found and corrected Quartz's `100vw` scrollbar offset; standard and focused reading now keep their 16px inline content gutters with zero horizontal scroll. Long Chinese titles preserve phrase-level breaks, and the asynchronously hydrated Explorer now marks the active item with `aria-current=page`, a full title and an unambiguous acid current state. Dynamic-content review found a real generated Mermaid SVG failure: the XML-invalid bare data attribute was corrected, and the self-contained diagram now renders on a high-contrast neutral canvas at desktop and mobile widths. Search empty state, mobile bounded result card, desktop split preview, Graph modal and Mermaid lightbox were all rechecked. Each checked state had no horizontal overflow; drawers and dialogs applied `role=dialog`, `aria-modal`, document scroll lock, inert background and focus restoration. A later visual audit found that `PosterFrame` relied on implicit CSS Grid placement: a persisted focused-reading preference could place homepage content after collapsed tracks. Explicit masthead/navigation/stage/utility grid areas now hold the poster frame together in standard and focused modes. Focused reading also provides a truncated running title; on narrow phones the `PFN` wordmark abbreviation reserves a stable title slot rather than shrinking both labels into illegibility. Normal rich-content callouts retain their Quartz icon and text label in addition to colour. A client-side `error` handler now replaces an article image that cannot load with a labelled, alt-text-preserving fallback card instead of leaving the native broken-image glyph. Finally, direct article links now recover only while Quartz asynchronously hydrates Explorer; its programmatic smooth scroll cannot cover the anchored heading, while pointer, touch and keyboard input immediately cancels recovery. The Table of Contents exposes exactly one `aria-current=location`, selected at the active reading line rather than the first of Quartz's overlapping `.in-view` markers.
  - Latest content-first refinement: the desktop tool rail now puts the outline before the global graph, suppresses a redundant document-title entry when sectional headings exist, and gives the current section a stronger location signal. Outline targets measure at least 40px on desktop and 44px on mobile; the active outline control is unavailable when the current route has no outline and its label/`aria-expanded` state now follows the actual rail or drawer. The persisted focused-reading preference is applied by a parser-time bootstrap before the masthead mounts. A real 390×844 regression found the narrow breakpoint's baseline `display: none` won against the drawer rule; the final narrow-screen rules explicitly restore both navigation and outline drawers. Both opened as 343px panels with no horizontal overflow. The global Graph control now has a clear 76×44px target and its real `role=dialog`, `aria-modal` state was rechecked on mobile.
  - Detail-pass follow-up: Quartz wraps Theme API styles in its base cascade layer, so the theme now emits an immutable local cascade asset and links it from its own header after Quartz defaults; this restores the intended sticky 68px header and fixed reading rails without changing the renderer architecture. The SPA client re-establishes the body-level progress indicator and backdrop on each navigation. The header is now an opaque foreground plane, inactive outline links retain normal text contrast, and standalone article images announce themselves as keyboard-operable preview triggers. In a current real long-form Chrome check, a physical Outline click opened from `scrollY = 1608` with `body.top = -1608px`; Escape closed it back at exactly `scrollY = 1608`, while `scrollWidth = clientWidth = 1352`.
  - Dynamic-state production follow-up: an independent read-only review found no P0, but did find a persistent route-transition brand flash plus two mobile Search regressions. The masthead now uses real long/compact mark nodes and a dedicated paint context instead of a pseudo-element and nested clipping. At 390×844, Search reserves a fixed 62px title/close bar; only the result list scrolls, the close target is at least 44×44px, and hover/focus/keyboard-selected cards explicitly move all text to ink on the acid selection ground. The latest HAT rebuild consumed the newly packed local archive and completed with 97 files, 2 assets, 20 articles and zero warnings. Human visual retest remains required.
  - Keyboard/touch detail follow-up: the sticky masthead clips route-swap paint by design, so its `:focus-visible` ring now sits within each target rather than losing its top and bottom edge at the header boundary. Drawer controls and the native Graph dialog CLOSE control are explicitly at least 44×44px. This is a local interaction refinement, not a change to the content-first visual language. The refreshed package is 27.3 kB and the real Quartz LAN rebuild again emitted 97 files, 2 assets and zero warnings.
  - Rich-content interaction follow-up: a real long-form inspection found Quartz already makes code blocks keyboard-focusable, so no redundant code override was added. A genuinely overflowing `.table-container`, however, had no keyboard region or touch affordance; overflow is now detected at runtime, then receives a focusable labelled region and a minimal `SCROLL TABLE` cue. Collapsible Obsidian callouts were rendered as clickable but non-focusable divs; their existing renderer-owned click toggle is retained, while the theme adds `role=button`, `aria-controls`, live `aria-expanded`, 44px target size and Enter/Space activation. The real Quartz rebuild consumed the 28.2 kB package and remained at 97 files / 2 assets / 20 articles / 0 warnings.
  - Discovery-surface detail follow-up: an independent source review found Quartz tag routes still rendering its unthemed `.page-listing` / `.section-ul` grammar, while a folder page retained an empty counter shell after the privacy-preserving controlled-list patch. The theme now owns the existing tag-list DOM with compact count metadata, scan-friendly separated rows, 44px article links, keyboard-visible row state and tag chips; on the 24-tag overview its repeated group headings are compact 44px index rows rather than display-size article headings. It hides only a `PageList` that contains no `.section-ul`, leaving the route-manifest-controlled folder Markdown list intact. The local package is 31.1 kB; static LAN output contains the cascade rules and the real Quartz rebuild again completed with 97 files / 2 assets / 20 articles / 0 warnings. Human visual confirmation of tag discovery at desktop and iPad widths remains required.
  - Progressive-discovery and desktop-search follow-up: a second independent audit measured the tag overview at 24 repeated `PageList` groups and 43 list items, with 41 article links mapping to only 17 unique articles. The theme client now converts only that overview into native closed `<details>` groups: the compact heading remains a direct link to the tag route, while its count/summary toggles the repeated article list with keyboard-native disclosure semantics. It does not CSS-hide focusable content. Separately, the portalled desktop Search is now an active-only grid: title/CLOSE and query stay fixed while results and preview own their individual scroll regions, matching the existing mobile contract. The local package is 32.3 kB; the real Quartz rebuild again completed with 97 files / 2 assets / 20 articles / 0 warnings. Human Chrome/iPad review must confirm the progressive groups and a long desktop search result list after a fresh reload.
  - Dynamic-overlay hardening follow-up: an independent UI review found no P0 in the real Graph happy path, but two P1 state-contract gaps. The graph now owns a labelled live loading region until Quartz appends its canvas; empty data and content-index failure use the same `/static/contentIndex.json` source that Quartz consumes, while Quartz's documented graph-library failure text is observed as a real renderer failure signal. The adapter deliberately does not invent a fixed loading timeout or alter Quartz's renderer/data model; a failure retains the dialog with an explicit retry that asks the native Graph control to render again. Image previews now remove the source image's interactive semantics from their clone, leaving Close as the only normal focus target; the preview itself exposes loading and alt-text-preserving failure feedback. A final independent re-review caught and corrected two follow-on keyboard paths: retry now moves focus from its soon-to-be-replaced status subtree to Graph Close before restarting, and a failed original image removes all preview-button semantics before it is hidden. In a fresh Chrome run against the rebuilt LAN output, Graph opened with `role=dialog` / `aria-modal=true`, displayed one real canvas and removed the transient status on success; the image preview clone retained only `alt`, and its only focusable control was Close. The local package is 37.1 kB; the real Quartz rebuild again completed with 97 files / 2 assets / 20 articles / 0 warnings. Human HAT should still exercise the visibly labelled empty/error paths under controlled network/data conditions.
  - Reading-flow hardening follow-up: an independent review found two P1 paths in the content-first shell. A navigation or outline link must own its resulting route/hash position, so the body unlock and position restoration now accept an explicit destination mode; Quartz's window-level router can stop document bubbling, therefore generated navigation and outline links receive direct lifecycle-safe handlers before the router runs. Any active outline is a modal/drawer (desktop rails never become an overlay), so selecting a heading now always releases it. Browser Back and a breakpoint resize recover focus through a visible matching masthead control, then the labelled article main as fallback; the restore waits one frame so the browser cannot overwrite it after `popstate`. Fresh Chrome proved the critical route: focused reading opened at `#0200--建立不变量`, then selecting `#0315--第一次修复` left `data-overlay=none`, the main non-inert, target top `72.2px`, `scrollY=1609.5` and zero horizontal overflow. A real Back close then returned focus to `展开本文目录`, with the main non-inert and the same reading position. The local package is 37.6 kB; the real Quartz rebuild again completed with 97 files / 2 assets / 20 articles / 0 warnings.
  - Reading-history and Search-route follow-up: a final independent dynamic review found one P1 path: a Search result was closing the sheet through the generic dismissal path, which could restore the previous document's scroll position after Quartz began its new-route transition. Dynamically rendered Search result anchors now receive the same destination-owned direct lifecycle binding as navigation and outline links: they close Search without scroll restoration, focus restoration or a competing history action. Reading history now persists both the precise `history.state` position and a same-tab `sessionStorage` fallback, used only for browser back/forward or BFCache recovery; both `0` and a non-zero reading location are valid positions. Fresh cache-bypassed Chrome verified a root route returns to `scrollY=0` after Back, while a long-form route returned to its actual saved `scrollY=3785` after a further navigation. It also opened desktop Search as a labelled inert modal, queried `低潮`, and selected the real low-tide result: the new route loaded at `scrollY=0`, `data-overlay=none`, main non-inert and no horizontal overflow. The final independent re-review found no remaining P0/P1/P2. Theme package tests passed 10/10; the repacked 39.0 kB local archive (190.4 kB unpacked) rebuilt real Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. The full root verification passed: 77 test files passed, 5 skipped; 675 tests passed, 8 skipped.
  - Content-first interaction detail follow-up: a later independent Chrome review found no P0, but three content-interrupting P1 paths. Quartz's active deep Explorer row could programmatically scroll the entire desktop rail below the masthead, hiding NAV, Search and the mode control; the standard expanded rail is now a fixed utility shell and only `.explorer-content` owns deep-tree scrolling, with a bounded client-side recovery for late hydration. Focused reading had visually hidden rails whose Quartz descendants could re-enter the Tab path; focused rails now retain `inert` and `aria-hidden` until their matching drawer is deliberately opened, after which only the active drawer is released. Finally, Quartz Search preview was an entire interactive note nested in the dialog. It is now a read-only theme projection with exactly one title, location, de-duplicated tags, one bounded excerpt and one `OPEN ARTICLE` action; the source preview remains hidden from visual and focus order. A stale native Search panel is also hidden unless `data-overlay=search` owns it. Fresh Chrome at 1352px verified a deep current page keeps NAV, PUBLIC INDEX, Search, theme mode and Explorer label visible; focused mode keeps both rails inert, opening Outline releases only its labelled modal drawer, and Tab after EXIT reaches the visible article Home link rather than an off-screen rail. Search `低潮` displayed the one-action summary, contained no visible raw-preview targets and opened the low-tide route at `scrollY=0` with Search closed, main non-inert and zero horizontal overflow. The Chrome connector did not advance native Tab focus for a complete Search loop, so human HAT-P1-002 must still tab through `input → results → OPEN ARTICLE → CLOSE`; source focus-trap filtering explicitly excludes `[hidden]`, `[aria-hidden=true]` and `[inert]` descendants. Independent re-review found no remaining P0/P1. Theme package tests passed 10/10; the final local archive is 42.3 kB (205.4 kB unpacked) and Quartz emitted 97 files / 2 assets / 20 articles / 0 warnings. Full verification passed: typecheck, lint, 77 passed test files / 5 skipped, 675 passed tests / 8 skipped, package and `git diff --check`.
  - Search-entry parity follow-up: after making stale Quartz Search panels fail closed outside `data-overlay=search`, the visible Explorer Search button needed to remain a first-class route into the same state machine—not a dead native control. Its captured click now opens the theme-owned dialog, while the internal native activation is explicitly permitted to populate Quartz results. Fresh Chrome clicked the actual left-rail `搜索` control: Search opened as the labelled modal, input received focus, the article became inert and no horizontal overflow appeared. Closing returned focus to that exact native rail button, removed main inertness and hid the panel. Theme package tests passed 10/10; the 42.5 kB local archive (206.4 kB unpacked) rebuilt real Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. Full root verification passed: typecheck, lint, 77 passed test files / 5 skipped, 675 passed tests / 8 skipped, package and `git diff --check`.
  - Search-hydration handoff follow-up: the captured native Explorer button now also handles the small route-render window before Quartz has mounted `.search-container`. The first trusted native click is allowed to create its DOM; a microtask then transfers ownership to the same theme dialog without issuing a second native toggle. When the panel already exists, header FIND and native Search still share the ordinary state-machine path. Fresh Chrome on the rebuilt LAN output again clicked the visible native control and observed `data-overlay=search`, a labelled modal, focused query input, `aria-expanded=true`, inert main and zero horizontal overflow. Theme package tests passed 10/10; the 42.6 kB local archive (206.8 kB unpacked) rebuilt real Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. Full root verification passed: typecheck, lint, 77 passed test files / 5 skipped, 675 passed tests / 8 skipped, package and `git diff --check`.
  - Evidence: [`Chrome iPad long-form`](./human-artifacts/HAT-P1-001-rich-longform-ipad-chrome.png), [`desktop rich home`](./human-artifacts/HAT-P1-001-rich-home-desktop.png)
  - Preview routes: `/`, `/notes/dispatches/long-form/`, `/notes/reference/markdown-kitchen-sink/`, `/notes/reference/media-layout/`, `/notes/atlas/region-%CE%B1/district-07/block-c/observation/`.

- [ ] HAT-P1-002 — 200%、纯键盘和 reduced motion
  - Status: READY
  - Human result: pending

- [ ] HAT-P1-003 — Repair、损坏、回滚、默认主题和卸载
  - Status: READY
  - Human result: pending

- [ ] HAT-P1-004 — npm 精确版本与离线缓存
  - Status: BLOCKED
  - Human result: package is not published to npm

### P2

- [ ] HAT-P2-001 — Cloudflare Direct Upload
  - Status: SKIPPED
  - Human result: no external-write authorization

## Follow-ups

- [ ] Record the human result for HAT-P0-002.
- [ ] Retest HAT-P0-002 with the rebuilt pathless-file candidate.
- [ ] Record visual and accessibility results for HAT-P1-001/002.
- [ ] Publish the external theme package before HAT-P1-004.

## Source Manifest

### Sources

- [`guide.md`](./guide.md): prepared checklist, accounts, data and acceptance criteria.
- [`../../CUSTOM-QUARTZ-THEME-SPEC.md`](../../CUSTOM-QUARTZ-THEME-SPEC.md): Theme API and AC-TH-01..15.
- [`../../BRUTALIST-QUARTZ-THEME-DESIGN.md`](../../BRUTALIST-QUARTZ-THEME-DESIGN.md): poster/editorial/instrument visual contract.
- [`../../BRUTALIST-THEME-PRODUCTION-SPEC.md`](../../BRUTALIST-THEME-PRODUCTION-SPEC.md): content-first shell, overlay state and production visual acceptance contract.
- [`../../src/content/mermaid.ts`](../../src/content/mermaid.ts): safe Mermaid asset generation used by the rich-content fixture.
- [`../../external-themes/brutalist/src/index.js`](../../external-themes/brutalist/src/index.js): explicit poster-frame landmarks and the focused-reading title context.
- [`../../external-themes/brutalist/src/client.js`](../../external-themes/brutalist/src/client.js): route-safe focused-reading context, inert focused rails, stable deep-Explorer scrolling, parity for header and native Explorer Search triggers, destination-owned drawer/Search lifecycle, read-only Search projection, durable Back/BFCache reading-position recovery, Quartz Graph state adapter and accessible image-preview lifecycle.
- [`../../external-themes/brutalist/src/styles/shell.css`](../../external-themes/brutalist/src/styles/shell.css): explicit PosterFrame grid areas and focused-reading title context.
- [`../../external-themes/brutalist/src/styles/responsive.css`](../../external-themes/brutalist/src/styles/responsive.css): narrow-screen focus-bar allocation.
- [`../../external-themes/brutalist/src/styles/overlays.css`](../../external-themes/brutalist/src/styles/overlays.css): Search and Graph dialog hierarchy, state presentation and mobile scroll ownership.
- [`../../external-themes/brutalist/src/styles/navigation.css`](../../external-themes/brutalist/src/styles/navigation.css): content-first outline hierarchy, graph trigger and mobile drawer cascade.
- [`../../external-themes/brutalist/src/styles/article.css`](../../external-themes/brutalist/src/styles/article.css): content primitive, image-failure and Quartz tag-discovery presentation rules.
- [`../../external-themes/brutalist/scripts/build.mjs`](../../external-themes/brutalist/scripts/build.mjs): deterministic local cascade-guard asset generation.
- User request on 2026-08-03: update the test Vault for human acceptance.
- User request on 2026-08-03: add deeply nested, mixed-length, full-format and image-text Markdown fixtures, then expose a LAN preview for iPad acceptance.
- User direction on 2026-08-03: production UI must receive Apple-level detail work; content is first-class, and drawers/sidebars must support focused reading.
- User direction on 2026-08-04: evaluate dynamic states as well as static output; sidebars must collapse to protect focused reading space.

### Produced artifacts

- [`human-report.md`](./human-report.md)
- `test-vault/.obsidian/plugins/pages-publish/{main.js,manifest.json,styles.css}`
- `test-vault/.obsidian/community-plugins.json`
- [`human-artifacts/HAT-P0-002-pathless-file.png`](./human-artifacts/HAT-P0-002-pathless-file.png)
- [`fixtures/vault-content/`](./fixtures/vault-content/): 16 deterministic rich-content notes plus 3 SVG assets; together with the four original visibility fixtures they form the 20-note Vault baseline.
- [`../../scripts/build-hat-lan-preview.ts`](../../scripts/build-hat-lan-preview.ts): real Quartz HAT builder using a HAT-owned theme trust environment.
- [`human-artifacts/HAT-P1-001-rich-longform-ipad-chrome.png`](./human-artifacts/HAT-P1-001-rich-longform-ipad-chrome.png)

### Key decisions

- The controlled HAT Vault receives only the packaged three-file candidate.
- The external theme remains a Vault-local `.tgz`; it is not copied into the plugin bundle.
- A fresh HAT Vault starts on Quartz default; it does not pre-activate executable theme code before human trust confirmation.
- Human acceptance starts at HAT-P0-002 because automated P0 isolation/build checks already passed.

### Verification evidence

- `prepare.sh prepared` checks byte identity between the release and Vault plugin files.
- `prepare.sh prepared` also checks every rich-content fixture byte-for-byte; current baseline is 20 Markdown notes and 3 non-Markdown assets.
- Real Quartz LAN build: Quartz 5.0.0 / `pages-publish-quartz-5.0.0.2`, 20 scanned articles, 0 warnings, 97 files, 2 generated assets.
- HTTP/discovery smoke: five representative routes return 200, private route returns 404, and private/unlisted canaries are absent from home, content index and sitemap.
- Chrome responsive smoke: 820×1180 long-form `scrollY = 0`; heading deep-link target top = 23.3px; three local SVGs decoded successfully.
- Chrome production-shell smoke: 1440×1000 standard/focused reading and native Graph dialog, 1024×768 compact rail/image dialog, 820×1180 drawer, 390×844 drawer/search and 320×844 standard/focused reading all used real Quartz output with no horizontal overflow. The Graph dialog was portalled to `body`, labelled and modal, then closed without reopening. At 320px, `html` uses a real content-width rather than Quartz's scrollbar-inclusive `100vw`, the wordmark and abbreviated `TOC`/`READ` controls fit the masthead, and article content stays 16px from each document edge. Mobile Search focused its input; Navigation and Search returned focus to their triggers; the fixed mobile Explorer no longer overlapped the drawer close control.
- Chrome dynamic-content smoke: Mermaid SVG assets are XML-valid, decode at 656×182px and render in a neutral high-contrast panel at 1440px and 390px without overflow. A mobile search no-result state is clear; an actual result is limited to a four-line text excerpt with its preview hidden, while the 1440px dialog displays a bounded two-column results/preview layout. The Graph canvas opened as a labelled inert modal and the Mermaid lightbox returned focus on close.
- Chrome PosterFrame/focused-reading smoke: after a persisted focus preference, home and long-form routes rendered their masthead and stage in explicit grid areas at 1352px, 820px, 390px and 320px. Every checked route had `scrollWidth = clientWidth`. At 390px the focused bar measured 48px high, exposed the current article title in a 166px truncated context slot, and retained only TOC and Exit controls; at 320px it retained a 96px title slot and the `PFN` wordmark abbreviation without overflow.
- Chrome content-detail smoke: real Quartz callouts retained their labels and icons in addition to their success/danger signal colours. Dispatching a real article-image `error` event replaced the image with a `role="img"` fallback card whose accessible label and visible copy preserve `alt` text. At 1352px it measured 717×120px; at 390px it measured 358×120px and the document remained `scrollWidth = clientWidth`. In light mode the card retains its pink signal rule but uses an ink-colour label (`rgb(21, 21, 21)`) rather than low-contrast pink text.
- Chrome deep-link/TOC smoke: after the real asynchronous Explorer hydration on a fresh document, `#0200--建立不变量` landed below the sticky header in standard reading (heading top `92.1px`, header bottom `68px`) and persisted focused reading (heading top `71.9px`, header bottom `48px`). Each state exposed exactly one TOC `aria-current=location`, `02:00 / 建立不变量`, with no horizontal overflow. A `PageDown` during the focused-recovery window left the page at `scrollTop = 1848` / heading top `−563.1px`, proving keyboard reading was not snapped back to the anchor.
- Chrome content-first/dynamic-state smoke: at 1352px the outline precedes Graph in the right rail; its active item is `00:40 / 发现`, its duplicate H1 is suppressed, and its links are 13.12px with a 40px minimum target. Focused reading correctly collapses both desktop rails and reports `aria-expanded=false`; opening Outline creates a 360px panel. On a real 390×844 emulation, navigation and outline drawers both opened as 343px visible panels, outline targets measured 44px, Graph exposed a 76×44px `OPEN` trigger, and `scrollWidth = clientWidth`. The real Graph dialog was then opened from that drawer with `role=dialog` and `aria-modal=true`, again without horizontal overflow.
- Chrome late detail-pass smoke: the unlayered theme-owned cascade asset is linked after Quartz defaults, giving a sticky 68px header, 256px/232px sticky desktop rails and `scrollWidth = clientWidth = 1352`. The SPA client retains one body-level reading-progress element and one backdrop after route changes. A physical current-route Outline interaction from `scrollY = 1608` locked at `body.top = -1608px` and Escape restored `scrollY = 1608` exactly. The header background computed as opaque `rgb(30, 30, 28)` and inactive desktop outline links retained opacity `1`.
- Latest dynamic-state rebuild: independent Chrome review exercised desktop route switching and mobile Search empty/input/results/hover/scroll/Escape states. It found the three regressions above; source-level theme tests now cover the repaired real mark structure, fixed mobile Search shell, persistent exit target and selected-result contrast. The rebuilt real Quartz output consumed the 27.0 kB local theme archive and emitted 97 files / 2 assets / 20 scanned articles / 0 warnings.
- Latest keyboard/touch detail rebuild: static source and real desktop Chrome state confirmed the updated masthead mark structure, `overflow: visible` paint isolation and zero horizontal overflow at 1352px. The browser session did not expose a working screenshot command or synthetic action state transition, so final human Chrome/iPad confirmation of the focus ring and Graph close target remains part of HAT-P1-001/002. Theme package tests passed 9/9; the 27.3 kB local archive built into real Quartz output with 97 files / 2 assets / 20 scanned articles / 0 warnings.
- Latest rich-content interaction rebuild: real desktop Chrome inspected the kitchen-sink article at 1352px. It confirmed a 717px code block with `tabIndex=0`, a renderer-owned horizontally scrolling table wrapper and a collapsed callout title with `cursor:auto` before the corrective source change. The rebuilt output contains the table-region/callout-semantic client code and content styles. Theme package tests passed 9/9; the 28.2 kB archive built to 97 files / 2 assets / 20 scanned articles / 0 warnings. Because the browser connection only retained the prior document client, final keyboard and 390px visual confirmation remains a human HAT step.
- Latest discovery-surface rebuild: an independent source-only audit of generated `/tags/dispatch/` found Quartz `.page-listing > .section-ul > .section-li > .section` markup and no prior theme selector for that surface. The repair keeps the renderer-owned semantic links, gives tag rows a 44px title target, compacts the repeated 24-tag overview headings and hides only an empty folder `PageList` shell. A second audit found the overview’s unbounded expansion; it now uses native closed disclosures with a retained direct tag route per group, removing repeated article/tag links from the initial Tab path. Search’s desktop dialog now uses the same fixed-control/independent-scroll ownership as its mobile sheet. Theme package tests passed 10/10; the 32.3 kB archive was rebuilt into real Quartz output with 97 files / 2 assets / 20 scanned articles / 0 warnings. Chrome checked the first-level `/tags/markdown/` output at 1352px: 4px list rule, 44px article links, 32px tag chips and zero horizontal overflow. The connected document retained its earlier client asset, so human fresh-load Chrome/iPad review of `/tags/` disclosures and a long Search result list remains a HAT step.
- Latest dynamic-overlay rebuild: independent review found no Graph happy-path P0, then approved the two state-contract P1 repairs. A final independent re-review found no remaining P0/P1 after the retry focus handoff and failed-original-image focus cleanup. `npm --prefix external-themes/brutalist test` passed 10/10. The repacked 37.1 kB local archive built into Quartz 5.0.0 with 97 files / 2 assets / 20 scanned articles / 0 warnings. The full `npm run typecheck && npm run lint && npm test && npm run package && git diff --check` suite passed (675 tests passed / 8 skipped). Fresh Chrome against the LAN server confirmed the Graph canvas success transition (`data-overlay=graph`, real canvas count 1, labelled modal, no remaining status layer and zero horizontal overflow) and image-preview semantics (clone is non-focusable/non-button and preserves `alt`; only Close is focusable). Source regression assertions cover focus handoff before retry and removal of preview semantics from a failed original image. Empty/error visuals are source- and state-tested but require controlled network or an empty content index for a human visual exercise.
- Latest reading-flow rebuild: independent review found and approved route/hash scroll ownership plus Back/resize focus recovery. Theme package tests passed 10/10; full `npm run typecheck && npm run lint && npm test && npm run package && git diff --check` passed (675 tests passed / 8 skipped). The 37.6 kB local archive rebuilt into real Quartz 5.0.0 output with 97 files / 2 assets / 20 articles / 0 warnings. A cache-bypassed fresh Chrome document in focused reading opened `#0200--建立不变量` at `scrollY=1234.5`, then selected `#0315--第一次修复` from the modal outline: it closed (`data-overlay=none`), removed main inertness, landed the new heading at `72.24px`, held `scrollY=1609.5` and had zero horizontal overflow. Opening the outline again and invoking browser Back also closed it, removed inertness, preserved `scrollY=1609.5` and focused the visible `展开本文目录` trigger. The final independent re-review found no remaining or new P0/P1.
- Latest history/Search-route rebuild: the independent review identified one Search-result lifecycle P1 and approved its repair. Each asynchronously rendered Search result link now owns its route transition before the Quartz router runs, so Search does not restore the outgoing page's scroll after the destination starts loading. Exact history positions are retained in `history.state` and mirrored into same-tab `sessionStorage` only as a Back/BFCache fallback; zero is intentionally retained as a first-class reading position. Fresh cache-bypassed Chrome verified root Back returns to `scrollY=0` (rather than the old page bottom), long-form Back returns to its saved `scrollY=3785`, and a real `低潮` Search result opens `/notes/field-guide/expeditions/coastal/low-tide/` at `scrollY=0` with Search closed, main non-inert and no horizontal overflow. The repacked local archive is 39.0 kB (190.4 kB unpacked) and the Quartz build remains 97 files / 2 assets / 20 articles / 0 warnings. Theme package tests passed 10/10. Full root verification (`typecheck`, `lint`, 77 passed test files / 5 skipped, 675 passed tests / 8 skipped, package, `git diff --check`) passed. The final independent re-review found no remaining P0/P1/P2 in the reviewed dynamic contracts.
- Latest content-first interaction detail rebuild: the independent review found three P1 interruptions and approved their repair: deep Explorer hydration scrolled desktop controls out of sight, CSS-hidden focused rails remained keyboard-reachable, and Search preview was a full second interactive article. In fresh Chrome at 1352px the deep current page retained NAV, PUBLIC INDEX, Search, theme mode and Explorer title within the rail (`scrollTop=0`, active row visible, zero horizontal overflow). Focused mode applies `inert`/`aria-hidden` to both collapsed rails; opening Outline releases only the right labelled modal while the left rail and main remain inert, and after closing it a physical Tab after EXIT lands on visible article Home rather than an off-screen rail. Search `低潮` now renders a 204px controlled summary with one title, four de-duplicated tags, an excerpt and exactly one focusable `OPEN ARTICLE` link; the original 2969px preview is `hidden`/`aria-hidden`. That link opened `/notes/field-guide/expeditions/coastal/low-tide/` at `scrollY=0`, Search closed, main non-inert and no horizontal overflow. The Chrome connector did not reproduce a full native Tab traversal from input through result/CTA/Close, so that last physical keyboard path remains an explicit human HAT-P1-002 check; the client focusable filter excludes hidden, `aria-hidden` and inert descendants. The final independent re-review found no P0/P1. Theme package tests passed 10/10; the 42.3 kB local archive (205.4 kB unpacked) built real Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. Full root verification passed (77 files / 5 skipped, 675 tests / 8 skipped; typecheck, lint, package and `git diff --check`).
- Latest Search-entry parity rebuild: the left Explorer `搜索` button is a native Quartz control while the masthead FIND is theme-owned. Once stale native panels were hidden outside `data-overlay=search`, the native button was explicitly captured and delegated to the same dialog lifecycle; the programmatic native activation is allowed through once so Quartz keeps populating results. Fresh Chrome clicked the visible native left-rail button and verified `data-overlay=search`, labelled modal semantics, input focus, inert main, no horizontal overflow and `aria-expanded=true`. Closing restored focus to that same native button, set `data-overlay=none`, removed main inertness and returned the panel to `display:none`. Theme package tests passed 10/10; the 42.5 kB local archive (206.4 kB unpacked) built Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. Full root verification passed (77 files / 5 skipped, 675 tests / 8 skipped; typecheck, lint, package and `git diff --check`).
- Latest Search-hydration handoff rebuild: the native rail trigger is now safe during Quartz’s small post-route mount window. If `.search-container` is absent, its first native event mounts the renderer DOM and a queued theme handoff opens the modal without toggling the new native state closed; existing panels still use the ordinary captured path. Fresh Chrome against rebuilt output clicked the actual rail Search and observed the labelled `data-overlay=search` dialog, focused `搜索内容...` input, native `aria-expanded=true`, inert article and zero horizontal overflow. Theme package tests passed 10/10; the 42.6 kB local archive (206.8 kB unpacked) built Quartz output with 97 files / 2 assets / 20 articles / 0 warnings. Full root verification passed (77 files / 5 skipped, 675 tests / 8 skipped; typecheck, lint, package and `git diff --check`).
- `npm run lint && npm test && npm run package` passed after the pathless-file fix: 82 test files, 675 passed / 8 skipped.
- Prior automated run: [`reports/20260803-185611/summary.md`](./reports/20260803-185611/summary.md).

### Open questions / risks

- Obsidian must reload after plugin files are updated.
- The pre-install fail-closed screenshot is expected security behaviour; the fixture ordering was corrected so it no longer blocks the start of HAT.
- npm and Cloudflare external cases remain unavailable until package publication and explicit authorization.
- HAT-P1-002 still needs one physical keyboard traversal of the Search dialog (`input → results → OPEN ARTICLE → CLOSE`); the connected Chrome automation surface did not advance native Tab focus, though DOM and theme focus-trap checks exclude the hidden source preview.
